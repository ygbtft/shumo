# 交付验证

完整记录见 [validation.json](../data/provenance/validation.json)。本次没有新建官方案例、发送官方测试请求或运行 mock 场景。

| 检查 | 结果 |
|---|---|
| Q1/Q2 算法测试 | 统一虚拟环境中 1301 项通过 |
| Q1 命令入口 | 标准输入成功产出结果、绘图数据与环境清单 |
| Q2 已有外层证书 | 独立复核有效，2660 个叶格 |
| Q3/Q4 核心算法与固定布局 | 21 个文件与两题第二次正式冻结包分别逐字节一致 |
| 全部正式记录 | 六份 jlog、结果元数据及请求轨迹哈希一致，清除数和时间重新核算通过 |
| 官方演练数据 | 927 局、199234 条有效请求复核通过 |
| 参数与消融配置 | 306 项构造检查通过，含联合扰动全部计划行 |
| Windows ARM64 已保存响应回放 | 27 局、5746 条命令匹配，最大位置偏差 0 米 |
| VM 部署与接口 | 运行包清单、Q3/Q4 配置、联合扰动构造检查通过 |
| 实验分组统计 | 927局逐局复算通过，各组样本、参数、均值和偏移逐项核对 |

入口直接连接官方HTTP。验证使用已保存记录，不创建新的官方案例。算法回放只证明既有轨迹上的一致性，不构成新增性能样本。

完整仓库可执行 `python tools/check_configs.py` 检查全部配置，`python tools/audit_data.py` 复核数据；回放需原 Windows ARM64 环境。正式三次汇总见 [正式数据说明](../data/formal/README.md)。

交付文件对应关系见 [交付核对](delivery-check.md)。
