# 问题1与问题2设计文档

版本：审计稿 v1，2026-09-10。本文只规定数学模型、实现合同与验证方法，不包含实现代码。后续 agent 应在本文通过用户审计后实现。

## 0. 范围、已确认决策与阅读依据

### 0.1 本轮交付与后续实现边界

- 本轮唯一项目产物为本文件，不修改 mock，不运行官方模拟器，不开发策略程序。
- 问题1接受任意有限数量 N 组“检测点、同一干扰源示向度”，输出角锥交集的类型、直径、最远点对、直径圆覆盖判定、最小覆盖圆和证明性见证。
- 问题2给出完整第二检测点候选集的解析定义、可计算判据，以及直接优化最坏情况定位直径的方法。90°只用于解释和生成搜索初值。
- 不实现问题3/4的完整搜索清除策略。与 mock 的连接限于观测转换、离线案例生成、局部两点实验及后续可调用接口。
- 不要求历史文件格式或接口兼容。新模块按本设计建立清晰合同，不迁移旧 baseline 的启发式阈值。

### 0.2 用户已经确认的决策

1. 问题1主区域是题面图2中的纯角锥交集；不使用圆形目标域、有效接收距离或 near 阈值裁剪它。
2. 问题2以有界误差下最坏情况定位直径为主指标；面积和 GDOP 为辅助；移动距离只在效果相当时择优，并绘制成本折中曲线。
3. 主候选集保证对所有与第一次观测一致的源位置及有效接收半径都收到信号，允许 near；保证第二条示向度的集合单列。空集不能隐瞒或替换为未经说明的启发式区域。
4. 理论示向误差半宽取 1°。接口适配层允许配置；保守舍入模式取 1.005°。附件是否将舍入计入 ±1°尚不明确。
5. 同地点重测误差固定；不同地点独立性、分布、空间相关性均不是题面保证。Fisher 分析必须显式标注额外统计假设。
6. 覆盖定理必须证明圆心被任一直径端点对强制确定；处理多个最远点对和无界区域；提供可由真实角锥构造的反例。

### 0.3 材料依据

| 来源 | 已核实内容 | 对本设计的影响 |
|---|---|---|
| [题面](../../B题.pdf)，第1页问题1/2，第2—3页附录1/2及图1/2 | 东为 x 正向，北为 y 正向；逆时针示向度；误差 ±1°；同点误差固定；源位于半径1800米圆域 | 坐标及角度合同；纯角锥主模型；问题2源先验约束 |
| 题面附录2(2)、(5)—(9) | 有效接收半径1000—1500米；5米内无示向度；20米内可光学定位清除；速度5米/秒；测量5秒 | 候选点保收信号判据；near分支；移动成本 |
| [附件2](../../附件/附件2.docx)，1.1—1.2、2.1—2.4、7.2—7.3 | 允许检测点在目标域外；每坐标分量绝对值≤2000000；返回direction/near/no_signal；示向度两位小数 | 不将检测点限在1800米圆域；观测适配及舍入模式 |
| [mock/geometry.py](../../mock/geometry.py) | bearing、angle_delta、distance、advance；与题面相同坐标约定 | 复用基本几何语义 |
| [mock/strategy.py](../../mock/strategy.py)、[后端合同](../../mock/backends/base.py)、[运行器](../../mock/client_or_inproc.py) | Action、Observation、next_action；Backend.send/exchange；观测不含真值 | 新解析核心不依赖后端；薄适配层接收公开观测 |
| [标定定位](../../mock/calibration/observations.py) | ±1.005°，预置方形，接收距离外包方形，顶点平均中心 | 仅作现有用途的参考，不作为问题1精确算法复用 |
| [误差场](../../mock/error_field.py)、[仿真器](../../mock/simulator.py) | 同点固定误差；多种合成误差场；先误差后两位舍入 | 合成对照，不当作官方误差模型事实 |

全文把“圆覆盖区域”解释为**闭圆盘包含区域**，不是只用圆周包含区域。

### 0.4 审计时需关注的新设计口径

问题1的主输出始终为纯角锥区域 P。问题2有目标圆域、首次收到信号等可用信息，故其主指标在本稿中明确选为**第二次反馈后的完整物理可行集 K 的直径**；同时输出纯角锥区域 P 的直径作对照，字段和图例不混用。

这是对“问题2定位直径指哪个集合”的具体化，不改变问题1已确认范围。理由：完整可行集才表示在所有已知信息下仍不能排除的位置；它也能统一处理 near。若审计要求问题2也只优化问题1的 P，则应整体切换到第6.8节定义的角锥目标，不能在数值结果中暗中替换。该项是本稿新增、需用户审阅的模型选择。

## 1. 数学对象与输入合同

### 1.1 记号

| 记号 | 含义与单位 |
|---|---|
| S_i=(s_ix,s_iy) | 检测点，米 |
| G 或 p | 源位置，米 |
| θ_i | 已读出的示向度；外部为度，内部为弧度 |
| ε_i | 示向误差半宽，默认π/180 |
| u(θ)=(cosθ,sinθ) | 单位方向 |
| cross(a,b)=a_x b_y−a_y b_x | 二维叉积 |
| D(c,r) | 以c为心、r为半径的闭圆盘 |
| A=D(0,1800) | 源所在目标圆域 |
| W_i | 第i次示向度的闭前向角锥 |
| P=∩W_i | 问题1纯角锥定位区域 |
| d(P) | 有界非空集合的直径；非空无界集合为+∞ |
| R(P) | 最小覆盖圆半径；有界非空时存在 |
| ρ | 某源固定但未知的有效接收半径，1000≤ρ≤1500 |
| F | 问题2第一次direction反馈后的源可行集 |
| C_sig、C_dir | 分别保证收信号、保证第二条示向度的候选集 |
| J(q) | 在第二检测点q的真实最坏情况后验直径 |

外部非有限坐标、角度、半宽均拒绝。理论几何输入不强加接口的±2000000限制；接口适配层施加该限制。内部长度统一米，不将经纬度直接传入。

### 1.2 任意N组输入

每条BearingMeasurement至少包含：position、bearing_deg、half_width_deg、measurement_id；可选channel、request_id、origin、rounding_mode。整个输入序列必须属于同一静止源、同一未清除阶段。

- N可为0，用于报告“无约束：全平面”；业务正常输入为N≥1。
- 不硬编码N=2。N=2只在问题2的解释与专门优化中出现。
- 几何API允许没有channel的手工输入；有channel时必须一致，不自动混合多个频道。
- 重复约束不增加信息。接口重复request_id先去重；同地点同频道同阶段的相同读数不作为独立样本。
- 同地点出现不同读数不一定立即判定区域空，但应发出数据一致性诊断；不靠假设独立噪声取平均来缩窄半宽。
- 主模型处理0≤ε≤90°。ε=0必须保留前向射线；ε=90°是半平面。ε>90°的角域一般不能由一个凸角锥表达，返回UNSUPPORTED_NONCONVEX_ANGLE，不偷偷取补角。

### 1.3 状态与空集约定

RegionKind取EMPTY、POINT、SEGMENT、POLYGON、UNBOUNDED。N=0作为UNBOUNDED的全平面子类。空集的直径字段为null，不输出0；虽然空集被任意圆盘包含，但业务覆盖判定为NOT_APPLICABLE。

点：d=R=0，圆心就是该点。线段：d为端点距离，R=d/2，覆盖判定成立。非空无界：数学直径+∞，不存在有限覆盖圆。JSON以status与null表达非有限量，不写非标准Infinity/NaN。

## 2. 问题1：纯角锥交会模型

### 2.1 角锥转半平面

令u_−=u(θ−ε)、u_+=u(θ+ε)、v=x−S。对0<ε<90°，

W(S,θ,ε)={x: cross(u_−,v)≥0，cross(u_+,v)≤0}。

另始终加入u(θ)·v≥0。对正常半宽它冗余，但可避免ε=0时错误地得到整条直线。所有约束统一为n·x≤b、||n||=1：

- 下边界：n=(sin(θ−ε),−cos(θ−ε))，b=n·S；
- 上边界：n=(−sin(θ+ε),cos(θ+ε))，b=n·S；
- 前向：n=−u(θ)，b=n·S。

角锥在S处包含顶点，是为得到闭集而采用的纯几何定义。设备在源点不返回示向度，此物理排除不放入P。

P是至多3N个闭半平面的交，因此是闭凸多面集。只有在非空、有界且二维时才称为凸多边形；不预设它一定是图2所示四边形。

### 2.2 可行性与无界性：禁止“大方框裁剪代替证明”

主流程先找可行点，再检测衰退锥，再对有界情况提取顶点。

**可行性算法：二维增量边界可行点法。**

1. 从p=(0,0)开始，按稳定顺序扫描约束。
2. 若p满足当前约束，继续；否则把p限制到当前边界L_i。
3. 用x=p_i+t v_i表示L_i，其中p_i=b_i n_i、v_i为n_i的90°旋转。
4. 对所有先前约束j，解(n_j·v_i)t≤b_j−n_j·p_i，累计t的上下界。
5. 系数为零且右侧为负则区间空；非零时按符号更新上/下界。区间非空时取最接近0的t，更新p。
6. 区间空意味着先前可行域与新半平面无交，返回EMPTY，并记录冲突约束。

正确性：原p违反新约束；若存在新可行点，连接二者的线段必在新约束边界上经过旧可行域，因此边界搜索不会漏掉可行解。最坏时间O(M²)、额外空间O(M)，M≤3N。高精度重算前不得因近零系数误报空集。

**无界性算法：衰退方向。**

Rec(P)={v:n_i·v≤0，对全部i成立}。P非空时，无界当且仅当存在v≠0属于Rec(P)。

- M=0直接返回全平面。
- 对每条边界的两个单位切向±rot90(n_i)检查全部齐次不等式。有合格方向即给出非零衰退见证v；p+t v对t≥0均在P中。
- 非平凡二维多面锥若不是全平面，至少有一个边界方向；此枚举涵盖射线、线、角锥及半平面，不能只检查x/y四轴。
- 未找到合格方向则Rec(P)={0}，区域有界。枚举最坏O(M²)。

以后可用角区间交替代枚举降低开销，但本版优先选择容易验证的分类算法。不将整个流水线误标成O(N log N)。

### 2.3 有界半平面交与顶点规范化

主顶点算法采用按边界方向排序的双端队列半平面交，时间O(M log M)。输入已通过可行性与有界性分类。

1. 使用有向边界使可行域在左侧；先按方向的半平面象限，再按叉积排序，避免以浮点“近似相等”破坏排序传递性。
2. 同方向平行约束保留更紧者。反向平行约束不合并：可能构成带、线或矛盾。
3. 新约束排除队尾两线交点时弹尾，排除队首两线交点时弹首；重复至满足。
4. 扫描完成后做首尾闭合清理，再取相邻边界交点。
5. 每个顶点必须复查所有原约束；去除重复点和同一直边内部点，保持逆时针、不重复闭合顶点。
6. 队列因共线、同线、零面积而不能给出可靠闭多边形时，进入独立的枚举后备算法，不把队列长度<3直接判成EMPTY。

**后备/验证算法：**枚举每对非平行边界交点，保留满足所有约束的点，再求凸包。对已证明非空有界的多面集必能得到点、线段或多边形。时间O(M³)，仅退化后备与小规模验证使用。

点/线段分类以仿射秩与高精度谓词为准；“面积很小”只标病态，不直接把细长多边形压成线段。

### 2.4 直径算法与证明

对凸多边形P=conv(V)，任意x=Σλ_i v_i、y=Σμ_j v_j有

||x−y||≤Σ_iΣ_j λ_i μ_j ||v_i−v_j||≤max_ij||v_i−v_j||。

因此最大距离可在顶点对中取得。

使用旋转卡壳枚举对踵顶点对：

1. V按逆时针排列，无相邻重复点，无直边内部点。
2. 对每条边(v_i,v_{i+1})，单调推进j，使三角形有向面积cross(v_{i+1}−v_i,v_j−v_i)最大。
3. 更新(i,j)、(i+1,j)的平方距离；面积相等时同时检查j和j+1，避免漏掉平行边对应的最远对。
4. j只绕多边形一周；输出平方直径、确定性选择的一组端点、端点索引。
5. 点与线段直接处理，不进入循环；只在展示时开方。

时间O(V)、额外工作空间O(1)。不默认收集所有最远对；多解时按规范化顶点索引选字典序最小对。小规模验证用O(V²)全对枚举，专门核对平行边与多解。

## 3. 问题1：覆盖定理、最小覆盖圆与反例

### 3.1 直径圆覆盖充要条件

设P非空紧凸，d=d(P)>0，A、B为任意一组满足||A−B||=d的端点，m=(A+B)/2。以下条件等价：

1. 存在半径d/2的闭圆盘包含P。
2. D(m,d/2)包含P。
3. D(m,d/2)包含P的全部顶点。
4. 每个顶点X都满足(X−A)·(X−B)≤0。
5. 最小覆盖圆半径R(P)=d/2。

**核心：圆心为什么被唯一强制确定。**若半径d/2、圆心c的圆盘含有P，则含有A、B，且

d=||A−B||≤||A−c||+||c−B||≤d/2+d/2=d。

所有不等式都取等号。两个距离各为d/2，三角不等式取等要求A、c、B共线且c在二者之间，故c=m。不存在换一个圆心就能挽救失败覆盖的可能。

圆盘凸，包含全部顶点等价于包含凸包。又

(X−A)·(X−B)=||X−m||²−d²/4，

得到Thales条件。任意覆盖圆还必须R≥d/2，因此条件5等价。

**实现判据：**优先检查全部顶点的Thales量，返回最大量及相应反例顶点。最小覆盖圆独立计算作为解释与交叉核对，不用两个近似半径相减作为唯一判据。

### 3.2 最远对非唯一、退化、无界

- 上述定理对任意最远对成立。若存在覆盖圆，每一组最远对的中点必须等于同一圆心。
- 如果两组最远对中点不同，立即证明不存在直径圆覆盖；但中点全相同本身不是完整判据，仍须检查所有顶点。
- 正方形有两条最远对，中点相同，覆盖成立；等边三角形三条最远对中点不同，覆盖失败。
- d=0时P为一点，单独返回半径0圆盘。不能用需要除以d的覆盖比值。
- 线段自动满足覆盖；无界区域返回UNBOUNDED、finite_cover=false，不绘制“无限半径圆”，也不借目标圆域截断后回答问题1。

### 3.3 Jung上界与失败时区间

任意非空紧集在平面上的最小覆盖圆由至多3个支撑点决定。若由2点支撑，R=d/2；若需要3点，圆心位于这3点凸包内，环绕圆心的三个相邻圆心角均不超过180°，其中至少一个≥120°。其弦长至少√3 R，因此d≥√3 R。

故d/2≤R≤d/√3；覆盖失败时严格有

d/2<R≤d/√3。

上界在等边三角形取得。注意：单独写R≤d/√3且d/√3>d/2，只说明这个上界不足以保证覆盖，**不能代替不覆盖的反例**。

补充证明义务：最小圆存在由紧性得出；若所有接触点位于某个开半圆内，可向接触点方向小移圆心并缩小半径，矛盾。因此圆心在接触点凸包内；平面凸包表示可选至多3点，得到支撑结论。

### 3.4 可由真实±1°角锥形成的等边三角形反例

取边长L=20米、ε=1°，

A=(0,0)，B=(L,0)，C=(L/2,√3L/2)，O=(L/2,√3L/6)，h=L cot(2ε)。

第一检测点S_0=(−h,0)，读数θ_0=ε。它的下边界为从S_0沿+x的射线，上边界方位为2ε。三角形内的点满足y≥0及

y≤√3L/2<L≤h tan(2ε)≤(x+h)tan(2ε)，

所以三角形完整包含在第一角锥内。第一角锥的有效下边界就是边AB的支撑线。

对k=0,1,2，绕O旋转第一角锥120°k：

S_k=O+Rot(120°k)(S_0−O)，θ_k=(ε+120°k) mod 360°。

三条下边界的内侧半平面恰好定义三角形ABC；三条上边界都不削去三角形。因此三角锥的交集**恰好等于ABC**。

令真实源G=O，则每个真实方向都位于对应±1°读数角锥内。L=20使检测点到O距离约583米，均大于5米且小于1000米，O位于目标圆域内。因此该例也能用全向源、任一合法有效接收半径产生合法direction观测。三个读数恰为1°、121°、241°，也适合两位小数接口。

该例：d=L，R=L/√3，面积√3L²/4。对最远对A、B，

(C−A)·(C−B)=L²/2>0。

论文同时显示三组检测点/读数、三角锥交集、以AB为直径的圆与最小覆盖圆。不能只画一个未经观测构造验证的三角形。

### 3.5 最小覆盖圆算法

采用Welzl随机增量算法，支撑集大小≤3；随机种子写入结果。小规模支撑解分别处理空集、单点、两点直径圆和三点外接圆；三点共线时检查能够覆盖三点的最远对圆，不能除以近零行列式。

递归的支撑集表示强制边界条件，不能随意把一个真正的三点边界子问题替换为任意三点最小圆。实现时使用显式栈避免Python递归深度随顶点数增长。遇到数值不一致，转高精度或小规模枚举后备。

输出center、radius、support_vertex_indices、最大包含残差、数值状态。完成后复查全部顶点，并检查圆是其支撑点决定的最小圆。随机增量固定维度期望O(V)，最坏O(V³)，栈空间O(V)。独立验证可枚举全部两点圆和非共线三点圆，筛选包含全部顶点者，最坏O(V⁴)，仅小案例使用。

算法资料：[CGAL最小覆盖圆文档](https://doc.cgal.org/latest/Bounding_volumes/classCGAL_1_1Min__circle__2.html)说明了至多3个支撑点与Welzl增量实现；本设计在Python中实现所需二维算法，不引入CGAL运行依赖。

## 4. 问题1边界情形与物理可行集扩展

### 4.1 必须明确区分的构型

| 构型 | 预期处理 |
|---|---|
| 一个正常角锥，或重复同一观测 | 非空无界 |
| 相同方向的不同检测点 | 可能无界或矛盾，按约束求解；不能凭中心射线平行作结论 |
| 相反方向、相向检测 | 可形成有界菱形/线段；不能把“平行”统一判无界 |
| 背向、无交的角锥 | EMPTY |
| 中心射线不交但角锥相交 | 必须输出真实交集 |
| ε=0 | 射线；可能点、线段、射线、空集 |
| 检测点共线 | 不直接判失败；取决于示向度和半宽 |
| 边界近乎平行 | 可能很长但有限，保留大直径并标病态 |
| 多边形远大于目标圆域 | 问题1仍报告纯角锥结果；另列物理扩展 |
| 全部约束正常但交集为空 | 提示读数不一致、半宽解释、频道/坐标错误等诊断，不能擅自放宽误差 |

### 4.2 单独的物理扩展

若所有输入是合法direction，则真实源应满足

G∈A，5<||G−S_i||≤1500，对全部i。

物理可行集F_phys=P∩A∩∩[D(S_i,1500)\D(S_i,5)]，其中减去的是包含边界的5米圆盘。有效半径ρ是同一源的固定未知参数；仅有收到信号的观测时，存在ρ等价于max_i||G−S_i||≤1500且ρ≥max(1000,max_i距离)。

- 目标域和1500米圆盘的交仍凸但可能有圆弧。
- 排除5米闭圆盘可能非凸、非闭；直径用上确界，不保证最大值实际取到。
- near反馈加入D(S_i,5)；对于已知全向源，no_signal意味着距离>ρ，需要保留同一个ρ的联合约束，不能逐条随意选独立半径。
- 对一般定向源，无信号还可能由朝向引起，不套用全向距离排除。
- 扩展输出独立字段physical_region、physical_diameter_bounds，不覆盖q1的polygon、diameter、coverage字段。
- 圆弧绘图离散只是显示，不能把离散内接多边形的直径当作精确物理直径。算法采用第7节的集合分支定界求上下界。

## 5. 问题2：源不确定集与完整候选区域

### 5.1 第一次读数给出的信息

输入S=S_1、θ=θ_1、ε=ε_1，且第一次结果明确为direction，源已知全向且尚未清除。

F={p∈A∩W_1:5<r_1(p)=||p−S||≤1500}。

联合世界集合为

Z={(p,ρ):p∈F，max(1000,r_1(p))≤ρ≤1500}。

这里r_1>5是严格不等式；边界5米应返回near。没有源位置概率先验。即使绘图使用F的闭包，也必须在数据中保留严格边界。

F为空表示输入物理不一致，问题2直接返回INCONSISTENT_FIRST_OBSERVATION，不给选点。

### 5.2 极坐标解析表示

令p=S+r u(θ+α)，α∈[−ε,ε]。目标圆域约束为

r²+2(S·u)r+||S||²−1800²≤0。

若Δ=(S·u)²+1800²−||S||²<0，该α无可行r；否则

r∈[−S·u−√Δ,−S·u+√Δ]∩(5,1500]。

该式处理S位于目标域外的情况，不预设S在原点。边界由两条角锥射线、目标圆弧、1500米圆弧与被排除的5米圆弧构成；各段保留来源标签。

### 5.3 保证收到信号的完整候选集

记r_min(p)=max(1000,||p−S||)，接口可达域Q=[−2000000,2000000]²。在没有障碍物和额外移动预算时，完整保收信号集合为

C_sig=Q∩∩_{p∈F} D(p,r_min(p))。

**充要性证明：**对给定p，首次direction只允许ρ≥r_min(p)。若||q−p||≤r_min(p)，任一允许ρ都覆盖q；若不满足，取合法ρ=r_min(p)，该世界在q无信号。对全部p量化即得到式子。

重要区别：∩D(p,1000)只是一个充分的保守子集，不是完整候选集；源距超过1000米时，首次收到信号已经排除了较小半径。

C_sig为闭凸集。F非空时，S∈C_sig，故无附加约束的原始集合必非空；选任意p_0∈F即有C_sig⊆D(p_0,r_min(p_0))，从而有界。使用该有物理证明的圆盘包围候选域，不使用任意绘图方框。

S可以作为“不移动、无信息增益”的基线保留在优化域中，但最终推荐第二个不同点；见第6.5节。要求S_2≠S后集合可能失去闭性，不强行声称最小值一定取得。

### 5.4 候选集的有限计算判据

将F按r_1≤1000与r_1≥1000分为F_L、F_H；在r_1=1000重叠没有影响。对h=q−S，定义

g_L(q)=sup_{p∈F_L}(||q−p||²−1000²)，

g_H(q)=||h||²−2 inf_{p∈F_H} h·(p−S)。

空子集对应的约束略去。则

q∈C_sig ⇔ q∈Q 且 g_L(q)≤0 且 g_H(q)≤0。

第二式来自||q−p||²−||p−S||²=||h||²−2h·(p−S)。它保留距离与角度耦合，是完整候选区域的解析描述；无需声称一定能化成一个圆或一个扇形。

**边界极值枚举器的合同：**

1. 将射线/圆之间交点、圆/圆交点和1000米分界交点列为事件，切成可行线段和圆弧。
2. 只保留实际F对应部分及其可达极限。若F整体只在r=5而无r>5的点，不能把这个孤立闭边界当成F的闭包。
3. 对g_L，平方距离最大值在边界：线段只查端点；圆弧p=c+R u(t)检查端点及u(t)与c−q同向的最远方向，若q=c则该弧等距。
4. 对g_H，线性函数极小值在边界：线段查端点；圆弧查端点及u(t)=−h/||h||的方向（若落在弧上）；h=0时值为0。
5. 返回最大违反量/最小支撑值及源位置见证。对开边界允许返回“上确界见证”，不得假称已取得。

F的边界曲线数为常数；完整边界构造最坏按曲线数K为O(K²)，构造后每个q检查O(K)个端点/驻点。绘图将C_sig写成上述隐式不等式的零水平集；高分辨率轮廓只是显示，成员判定调用解析极值器。

### 5.5 保证第二条示向度的更强集合

C_dir={q∈C_sig:对全部p∈F，||q−p||>5}。

等价于C_sig\∪_{p∈F}D(p,5)。它一般不凸。需要区别：

- dist(q,cl F)>5是方便验证的充分条件。
- dist(q,cl F)<5则不满足（对实际F的极限应保持可达性）。
- dist(q,cl F)=5必须检查等号是否在实际F取得。若仅由被第一次direction排除的r_1=5边界取得，仍可能保证direction。例如q=S，全部p∈F都满足r_1>5。

不得简单以“闭包距离>5”冒充精确充要条件。输出C_dir的精确逻辑定义、保守内集与数值不确定边界带。

### 5.6 可达性、移动预算与空集

无障碍、允许目标域外测量；默认可达域就是Q。为生成折中曲线可加预算||q−S||≤B，但完整无预算解必须单独计算。

- C_sig为空且F非空、Q含S：说明候选计算有错误；不能正常返回“信号无法保证”。
- C_sig∩其他附加约束为空、C_dir为空或只剩不移动点：分别报告原因。
- C_dir为空而C_sig非空：回退到允许near的主方案。
- 附加移动预算/人为禁区造成空集：先报告冲突，再由显式配置放宽预算/禁区。
- 若确需放弃保收保证，启用独立的risk_relaxed模式，在Q与预算域上建模no_signal分支和固定未知ρ；本稿只定义该扩展接口，不把它作为主方案悄悄使用，也不承诺无先验的“收信号概率”。

### 5.7 一个可手算的候选点验证

S=(0,0)、θ=0、ε=1°时，F={(r cosα,r sinα):5<r≤1500，|α|≤1°}，目标圆域不进一步裁剪。

取q=(750,400)。对F_L，最坏距离出现在r端点与角端点。r→5时距离约845.6米；r=1000、α=−1°时约486.6米；均小于1000米。对F_H，

min h·(p−S)=1000(750 cos1°−400 sin1°)，

因此g_H=750²+400²−2000(750 cos1°−400 sin1°)<0。故q属于C_sig。又q到W_1的距离为400 cos1°−750 sin1°>386米，故q∈C_dir。

该例证明候选集不只是一条示向线，也不需要把S_2放在半径1800圆域边界上。数字是解析式近似展示，实现验收以表达式为准。

## 6. 问题2：真实最坏情况目标及双指标解释

### 6.1 后验区域的精确定义

对q∈C_sig、q≠S，所有可能反馈分为：

- near：K_near(q)=F∩D(q,5)。只有该集合非空时该反馈可发生。
- direction，读数β：K_dir(q,β)={p∈F:||p−q||>5，|angle_delta(bearing(q,p),β)|≤ε_2}。
- no_signal：在主候选集与模型成立时不可能；若mock或实际数据出现，报告保证失配，不能把它当成正常成功选点。

首次方向误差已通过F条件化。理论主模式ε_1=ε_2=1°。K_dir一般不是多边形且可能非凸；不把问题1旋转卡壳直接用于它。

对非闭集合统一定义diam(K)=sup_{x,y∈K}||x−y||；空反馈分支不参与最坏情况。

J(q)=sup_{所有可实现第二反馈o} diam(K_o(q))。

它是反馈后最坏剩余空间歧义，不是定位估计器的均方误差，不是某次零误差观测的直径，也不是对源位置采样的平均值。

### 6.2 不可区分位置对的等价公式：主求解入口

对q≠S，两个源位置x,y∈F能产生相同near，当且仅当||x−q||≤5且||y−q||≤5。

两个源位置能产生相同direction，当且仅当二者都距q大于5，且二者真实方向的圆周最短夹角ψ满足ψ≤2ε_2。因为每个位置允许的读数区间半宽均为ε_2，两区间相交恰好满足该条件。

故定义

H_near(q)={(x,y)∈F²:||x−q||≤5，||y−q||≤5}，

H_dir(q)={(x,y)∈F²:||x−q||>5，||y−q||>5，ψ(q,x,y)≤2ε_2}，

就有

J(q)=sup_{(x,y)∈H_near(q)∪H_dir(q)}||x−y||。

**证明顺序：**先把某反馈的直径展开成对x,y的上确界，再交换两个上确界；“存在相同反馈”即上述不可区分条件。每个x、y可以对应不同的合法世界和不同ρ，符合不确定性比较，而不是要求现实同时存在两个同频道源。

保收信号已对所有p、ρ成立，所以本式不再显式优化ρ。不同地点只有有界误差而无已知相关规律，允许所有符合界限的第二读数是稳健建模，不能称其为概率独立假设。

0≤J(q)≤diam(F)≤3000米；near分支单独≤10米。与纯角锥目标不同，此处因物理先验有界，不会出现+∞。

### 6.3 无atan2的角度可行性判据

令a=x−q、b=y−q、w=a·b。主半宽满足2ε_2<90°，在direction距离下限成立时，

ψ≤2ε_2 ⇔ w≥0 且 w²−cos²(2ε_2)||a||²||b||²≥0。

这使内层最大化成为4个位置变量上的低维半代数约束问题，可做区间分支定界；不需要把r和φ当成互相独立的决策变量。

问题2主优化器支持0<ε_2<45°，超出范围返回不支持该多项式判据；问题1允许到90°的合同不意味着问题2也用同一角度分支。ε_2=0可单列严格共线同向退化测试，不作为默认全局求解模式。

### 6.4 最优点、次级移动距离和折中曲线

无预算主问题：J*=inf_{q∈C_sig,q≠S}J(q)。若最优值不取得，报告下确界和可达到该容差的点，不伪造唯一最优位置。

数值结果必须提供L≤J*≤U及已验证候选q_best；U来自该点的内层**上界**。给定定位容差τ_J，在已认证的近最优集合中再最小化||q−S||。

不把“直径+任意权重×移动距离”作为主目标。实际次级条件用J(q)的上界≤L+τ_J（必要时先收紧主求解gap），避免把未知最优值当成已知。

折中曲线定义

V(B)=inf_{q∈C_sig，||q−S||≤B}J(q)。

默认预算扫描0、50、100、200、400、600、800、1000、1500、2000、3000米，与解析C_sig最大可达距离比较后去除无意义重复预算。必须在每个预算的完整可行域求解，不只在预先挑的“90°轨迹”上搜索。

V(B)理论上随B不增。移动耗时=B_actual/5秒，同频道第二次测量另加5秒；预算不是一定消耗的距离。图同时显示上下界带，若区间尚重叠，不强行宣称某预算严格优于另一预算。

### 6.5 同点重测的特殊语义

q=S时第二读数与第一次相同，后验仍为F，故J(S)=diam(F)。不能允许独立第二误差并借两次同点读数缩小F。

优化器可将S作为已知基线，不允许最终把“再次测量S”描述为获得了新定位信息。没有已证明改善时返回NO_CERTIFIED_IMPROVEMENT，仍可输出一个不同点的近最优方案及其界限。数值“很近”不等于同一个地点；同点判断使用规范化后的精确坐标身份，数值病态另标。

### 6.6 集员面积近似：距离与夹角联合结构

在一个给定真实源G附近线性化，r_i=||G−S_i||，θ_i为真实方向，n_i=(−sinθ_i,cosθ_i)。小误差产生条带

|n_i·δG|≤r_i ε_i，

其中ε_i用弧度。两条带的平行四边形面积为

A_lin=4r_1r_2 ε_1ε_2/|sinφ|，φ=θ_2−θ_1。

固定r_1、r_2、误差半宽时，|sinφ|最大使面积最小，对应视线正交。移动S_2会同时改变r_2和φ；此外主目标是最坏直径，不是面积。不能把此公式当成全局选点目标或有限角锥交集的精确面积。

当角锥近共向、估计点接近5米阈值、先验边界主导、交集跨越源距尺度时，线性化不可靠。图中显示精确角锥面积或完整后验面积上下界与A_lin的相对误差，病态构型不绘成有效近似。

### 6.7 Fisher/GDOP辅助分析

仅在“不同检测点的方向误差独立、零均值高斯，方差σ_i²，角度单位弧度”的假想局部模型下，

I(G)=Σ_i n_i n_i^T/(σ_i²r_i²)。

两站非平行时

det I=sin²φ/(σ_1²σ_2²r_1²r_2²)，

tr(I^{-1})=(σ_1²r_1²+σ_2²r_2²)/sin²φ。

同方差σ时，RMSE的局部CRLB为σ√(r_1²+r_2²)/|sinφ|。约定无量纲GDOP=√(r_1²+r_2²)/(L_ref|sinφ|)，默认L_ref=1000米，并另列σ以免混淆单位。

固定距离时90°最优；不据此声称稳健主优化器的解必须正交。平行时信息矩阵奇异，GDOP=+∞；near分支不使用方位Fisher模型。σ=1°/√3只可作为额外“均匀误差等方差”的展示选择，不能一边假设有界均匀、一边不加说明套用常规高斯CRLB。主图建议明确写“高斯辅助模型，σ=指定值”。

对给定G绘图必须标其为假定/合成源真值。以F内几个源位置的GDOP曲线说明不确定性，不能只用F中心值冒充最坏界。

### 6.8 纯角锥目标对照与可能无界

为了衔接问题1，对每个可实现direction读数β另计算

P_ang(q,β)=W_1∩W(q,β,ε_2)，J_ang(q)=sup_{可实现direction β}diam(P_ang(q,β))。

其后验不使用F裁剪，可能无界，即使完整物理后验始终有界。near分支没有第二角锥，因此J_ang标为“direction条件下的对照”，不可把near伪造成角度0°。

固定q时，可实现β来自F\D(q,5)的可见方向集合在圆周上扩张±ε_2。可见角区间的事件由边界端点方向、圆弧切线方向、与5米圆的交点构成；逐角区间取中点，用射线与F的径向区间检验是否可见，边界严格性单独保留。

对这些β区间，四条角锥边界的交点是sinβ、cosβ的有理函数。按平行事件和三线共点事件分段，段内多边形组合结构固定；对每组顶点距离平方求驻点及端点单侧极限，取上确界。每个段先检查衰退锥，出现可实现无界构型或距离发散则返回+∞。不得只扫有限个误差端点。

若用户决定把J_ang提升为主目标，应连同near评价规则重新确认；本稿默认它是诊断与论文比较，不影响J的排序。

## 7. 问题2数值求解：上下界、全域搜索与停止条件

### 7.1 为什么需要两层全局求解

外层选择q，内层选择最不利的不可区分源位置对x,y。目标通常非光滑、非凸，最坏情形可能不在误差端点或F顶点。有限蒙特卡洛最大值只给内层下界，局部优化只给候选点，二者都不能证明全局最优。

本版采用：解析候选集成员/分离判据 + 四维内层区间分支定界 + 二维外层分支定界。先生成候选以便快速得到可用解，再用上下界收紧证明。预算耗尽必须报告UNRESOLVED_GAP，不能把“已运行足够久”当作最优性证明。

### 7.2 区间运算基础合同

所有用于证明的区间运算必须向外舍入，覆盖实数结果。具体选择`python-flint`的`arb`实数球运算，由单独的IntervalBackend封装成所需的闭区间合同；它以中点和误差半径包围真值，可调中点精度，见[官方arb文档](https://python-flint.readthedocs.io/en/latest/arb.html)。实现时锁定经测试的发行版本，不直接依赖开发版。普通NumPy float64、仅提高小数位数的mpmath或无误差分析地nextafter一次，均不能直接宣称提供严格区间证书。

矩形端点优先存为二进制有理数，二分操作保持精确；转换为arb时由精确中点和向上包围的半径构造。常数从原始十进制字符串或精确有理表示构造，不先经float舍入。导出上下界用有方向保证的端点，并在十进制序列化时再次向外舍入；不能只打印球中点。若要与普通float输入一致，则明确将该float所代表的二进制数作为精确输入。

默认工程探索模式可用float64/高精度，结果标NUMERICAL_BOUNDS；只有通过区间后端及原始角度常数包围才标CERTIFIED_BOUNDS。本文中的“上下界认证”均以该后端存在为前提；依赖不可用时照样可输出候选与数值结果，但不能升级认证标签。

需要的区间操作：加减乘除、平方、平方根、sin/cos、点积、叉积、min/max、与阈值的三态比较。区间除数含0时返回未定并细分，不强行置为0。角度常数π及cos(2ε)本身也用外包区间。

### 7.3 固定q的内层最坏直径

1. 构造包围F的矩形B_F；可由S±1500与目标圆外框相交获得，这是有证明的外包。
2. 分near、direction两条分支，各以B_F×B_F作为四维节点；空分支用−∞内部哨兵排除，输出不出现该值。
3. 对x、y矩形分别检查W_1、目标圆、r_1≤1500、r_1>5。若某约束区间已证明不可能，删除节点。
4. near再检查两个距q≤5；direction检查两个距q>5及第6.3节多项式条件。只有证明不可行才能删节点。
5. 节点目标上界为两矩形最远角点距离，即√(max|x_x−y_x|²+max|x_y−y_y|²)。这是对所有点对的上界，无需源点恰在角点。
6. 从节点中心、边界事件点及局部可行修正生成真实可行x,y。只有逐项验证成功的点对才能提高全局下界。direction为该点对构造共同读数（短角弧的中点）并验证误差界，作为可检查的最坏情形见证。
7. 优先处理上界最大的节点；分裂最长的归一化坐标边，保留约束来源和严格边界标签。
8. 全局U为未处理节点上界最大值与当前见证下界的较大值；L为已验证点对距离最大值。U−L≤τ_inner时结束。

可行见证生成不依赖黑盒优化器宣称可行：从第5.2节角区间的端点、中点及逐级二分角出发，将射线与当前x/y矩形、目标圆域和合法径向区间求交；取实际非空区间的中点、合法端点及逐级二分点，组成有限候选对，再调用verify_pair。对被排除的r=5端点只取向内点；对真实存在的孤立源点/边界段保留专门参数化。局部优化可帮助找到更长点对，但必须通过同一验证入口。这样即使尚未找到正直径见证，也可用同一点x=y的合法世界维持下界0，而不是伪造下界。

可利用x,y对称性只保留字典序x≤y的一半，但不是正确性必需条件。near上界可额外截到10米，全局上界截到F的已验证直径上界，增强剪枝。

**严格不等式：**r_1>5、r_2>5不能简单改成≥5并宣称完全等价。闭松弛可用于上界；下界必须有实际可行见证。若严格边界导致松弛孤立点或细分停滞，返回边界未决及gap；进一步使用边界事件/高精度可行性细分消除。不得因上界来自一个实际上不可能的边界点就把它报告成“真实最坏案例”。

平方距离可作为内部目标，避免重复开方；停止时将上下界转为米比较。

### 7.4 外层完整C_sig的空间覆盖

1. 由任意实际p_0∈F取得D(p_0,r_min(p_0))的外包矩形，与Q、预算圆外包矩形相交，作为根节点。全部C_sig都在其中。
2. 以解析候选成员判据验证所有试探点。首批点包括S、F边界/中心代表点对应的正交启发点、候选域边界点及均匀粗网格；这些只用于产生上界，绝不截掉其余区域。
3. 对不在C_sig的试探点，候选极值器给出违反圆盘D(p,r_min(p))。该圆盘是对全部候选点有效的切割约束。若整个q矩形到p的最小距离已大于半径，可安全删除矩形。
4. 多个圆盘切割与预算约束可用于凸可行性检查；只有验证了分离/不可行证据才能报告整块无候选点。单纯“中心不满足”不足以删掉整块。
5. 矩形四角都经解析验证属于C_sig时，由凸性可知整块都属于C_sig。跨边界的矩形继续保留/细分；不忽略薄边界或孤立可行点。
6. 对每个已验证候选q调用内层求解，使用内层U(q)更新外层最优上界U*。
7. 节点下界采用第7.5节的对所有q有效的不可区分对见证；默认下界0始终合法。
8. 按最低下界优先细分q矩形，剪掉下界≥当前最优上界的节点。保留边界相等候选用于次级最短移动，不随意删除多解。

### 7.5 外层下界：不能把内层上界方向用反

维护已经找到的实际源位置对库T={(x_j,y_j)}，每对都属于F²。在一个q矩形B内，若能用区间运算证明某对对**每一个q∈B**都可产生相同反馈，则

min_{q∈B∩C_sig}J(q)≥||x_j−y_j||。

两种充分证明：

- 对全部q，x_j和y_j都距q≤5，构成near共同反馈；
- 对全部q，二者都距q>5，且w≥0及角度多项式≥0，构成direction共同反馈。

在B中包含S时，不需要据“重新抽样误差”解释S：J(S)=diam(F)≥该点对距离，因此上述下界仍成立。对其他点使用正常证明。

取全部已认证点对距离的最大值作为L_B。来自某个点q_c的“内层最坏值”不能直接当作整个B的下界，必须经过上述全块验证。

每次获得新内层见证，把它加入全局T并更新相关节点；节点缩小时已有见证可能变成全块有效。边界最坏对可向可行内部扰动形成更容易认证的见证，但扰动后距离和全部约束必须重新验证。

全局L*=min_{所有未剪掉可行/未决节点}L_B（结合已经完成的节点记录）；始终保证L*≤J*≤U*。实际终止实现应保留已剪枝节点下界或使用当前U*规则，不能因为队列意外清空而丢失最优性信息。

### 7.6 精度预算与诚实的结果等级

建议默认：τ_inner=0.02米、τ_outer=0.10米；论文常规表格保留0.1米。这些为工程起点，可配置，不是题面规定。内外层容差须逐步收紧，避免对明显劣点浪费时间。

结果等级：

- CERTIFIED_EPS_OPTIMAL：完整域覆盖、候选保证和所有上下界均经验证，U*−L*≤τ_outer。
- CERTIFIED_BOUNDS：上下界有效，但gap未达标。
- NUMERICAL_CANDIDATE：候选及结果经过普通数值检查，没有严格最优性证书。
- INCONSISTENT_FIRST_OBSERVATION / EMPTY_ADMISSIBLE_SET / NO_CERTIFIED_IMPROVEMENT。

每个结果记录节点数、最大队列、内外层gap、容差、精度位数、截止原因、未决边界数、候选是否经过保收信号验证。不得把局部优化器success字段映射成CERTIFIED_EPS_OPTIMAL。

一般非凸半代数优化没有本设计可承诺的低阶多项式复杂度。若内层处理T_in个四维节点、外层T_out个二维节点、候选检查边界片数K，主要成本约O(T_out·T_in+K·T_out+见证库检查成本)，空间由两个队列和见证库决定；不能写成“恒定时间，因为维数固定”。退化边界可能导致大量细分，预算耗尽返回未决是合同的一部分。

### 7.7 面积辅助指标的计算

对已选q与具体反馈，后验面积用二维矩形分割：完全在K内的矩形面积计入下界，不能排除但未证明在内的矩形计入上界，按边界细分直到面积gap达标。圆弧Green公式积分可作为后续加速，不是主直径算法前置依赖。

若报告“最坏后验面积”，必须再对全部可实现反馈求上确界并给出界限；本版默认论文主图为指定合成真值及读数的后验面积，并明确标“条件面积”，不能把它与J的最坏直径放在同一条未标口径的色条上。

## 8. 模块化代码架构与关键类型

以下目录和签名是待实现合同，不表示本轮已经创建这些文件。

### 8.1 目录规划

```text
models/
  __init__.py
  q1q2/
    PLAN.md
    __init__.py
    types.py                 数据结构、枚举、配置
    numeric.py               谓词、尺度、区间后端、精度升级
    angles.py                角区间及环绕，复用mock基础方向语义
    halfplanes.py            角锥、可行性、衰退锥、半平面交
    polygon.py               规范化、面积、旋转卡壳
    enclosing_circle.py      Welzl、Thales判定、独立枚举核验
    q1.py                    问题1公共入口及结果装配
    physical.py              源可行集、线段圆弧边界、物理扩展
    candidates.py            C_sig、C_dir、极值器、分离见证
    posterior.py             第二反馈可行集、不可区分位置对
    robust_inner.py          固定q内层分支定界
    robust_outer.py          完整候选域优化、预算曲线、次级择优
    diagnostics.py           面积近似、Fisher/GDOP、J_ang对照
    adapters.py              手工输入、mock观测、JSONL转换
    examples.py              解析案例的数据构造说明对应实现
    experiments.py           局部离线实验与报告装配
    plots.py                 论文图；依赖matplotlib
    cli.py                   q1、q2、examples、figures入口
    tests/
    data/                    手工/解析输入，不存官方隐藏真值
    configs/                 数值与绘图参数
    outputs/<run_id>/         JSON、CSV、PDF、PNG及清单
```

依赖方向：types/numeric → 几何和集合模块 → q1/robust → adapters/experiments/cli/plots。几何核心不得反向导入运行器或仿真器。绘图不承担求解职责；论文图从已保存结果重绘。

### 8.2 数据类型

| 类型 | 核心字段 |
|---|---|
| Point2 | tuple[float,float]；认证模式可用区间/高精度内部对应类型 |
| BearingMeasurement | position、bearing_deg、half_width_deg、measurement_id、channel可选、origin、rounding_mode |
| HalfPlane | unit_normal、offset、measurement_id、boundary_kind |
| PolyhedralRegion | kind、vertices_ccw、feasible_point、recession_witness、active_constraint_ids、numerical_status |
| DiameterResult | status、distance_m、distance_sq_m2、endpoint_indices、endpoints、tie_policy |
| CircleResult | center、radius_m、support_indices、containment_residual、numerical_status |
| CoverResult | verdict=YES/NO/UNRESOLVED/NOT_APPLICABLE、forced_center、max_thales_value、witness_vertex、radius_ratio |
| Q1Result | input_summary、region、diameter、diameter_circle_cover、minimum_circle、diagnostics |
| SourceSet | first_measurement、arena、near_radius、receive_bounds、strict_boundary_flags、boundary_pieces、bounding_box |
| BoundaryPiece | kind=SEGMENT/ARC/POINT、端点或圆心半径角区间、端点开闭性、source_constraint_ids |
| CandidateCheck | verdict=IN/OUT/UNRESOLVED、g_low_bounds、g_high_bounds、violating_source、boundary_attainment |
| BoundInterval | lower、upper、units、certification、attained_lower、precision_bits |
| AmbiguityWitness | source_x、source_y、feedback_kind、common_bearing可选、distance、feasibility_residuals |
| InnerResult | point、diameter_bounds、branch_bounds、witness、node_count、unresolved_boundaries、stop_reason |
| Q2Result | chosen_point、candidate_guarantee、objective_definition、global_bounds、point_inner_result、movement、alternatives、pareto_results、status |
| NumericPolicy | 长度/角度/平方量容差、精度升级、IntervalBackend、seed |
| SearchConfig | inner_gap_m、outer_gap_m、area_gap_m2、node_limits、time_limit_s、movement_budget_m可选 |

所有null有明确含义，不用空列表同时表示“空集”“尚未计算”“无界”。Point/Segment顶点数分别为1/2；POLYGON至少3个非共线顶点。

### 8.3 关键函数签名与职责

以下为文档中的接口声明，不提供函数体。

| 模块 | 签名 | 输出保证 |
|---|---|---|
| angles | normalize_deg(angle: float) -> float | [0,360)，−0规范化 |
| angles | circular_intervals(center_deg: float, half_width_deg: float) -> tuple[AngleInterval,...] | 跨零拆分，内部拓扑标签保留 |
| halfplanes | wedge_halfplanes(obs: BearingMeasurement) -> tuple[HalfPlane,...] | 含前向约束 |
| halfplanes | find_feasible_point(hps: Sequence[HalfPlane], policy: NumericPolicy) -> FeasibilityResult | 点或不可行见证或未决 |
| halfplanes | recession_direction(hps: Sequence[HalfPlane], policy: NumericPolicy) -> RecessionResult | 非零方向或已证有界或未决 |
| halfplanes | intersect_halfplanes(hps: Sequence[HalfPlane], policy: NumericPolicy) -> PolyhedralRegion | 完整分类，不截断无界集 |
| polygon | normalize_vertices(vertices: Sequence[Point2], policy: NumericPolicy) -> tuple[Point2,...] | CCW、规范起点、去真冗余 |
| polygon | polygon_diameter(region: PolyhedralRegion, policy: NumericPolicy) -> DiameterResult | 卡壳；退化状态显式 |
| enclosing_circle | minimum_enclosing_circle(vertices: Sequence[Point2], policy: NumericPolicy, seed: int) -> CircleResult | 支撑与包含检查 |
| enclosing_circle | check_diameter_circle(region: PolyhedralRegion, diameter: DiameterResult, policy: NumericPolicy) -> CoverResult | 强制圆心、Thales见证 |
| q1 | solve_q1(measurements: Sequence[BearingMeasurement], policy: NumericPolicy) -> Q1Result | 任意N、同源检验 |
| physical | first_source_set(first: BearingMeasurement, physics: PhysicsConfig, policy: NumericPolicy) -> SourceSetResult | F及严格边界 |
| physical | build_boundary(source_set: SourceSet, radial_split_m: float \| None, policy: NumericPolicy) -> BoundaryArrangement | 合法线段/圆弧及事件 |
| candidates | check_signal_candidate(source_set: SourceSet, point: Point2, policy: NumericPolicy) -> CandidateCheck | 充要判据的三态判定 |
| candidates | check_direction_candidate(source_set: SourceSet, point: Point2, policy: NumericPolicy) -> CandidateCheck | 保收且排除near，包括等号取得检查 |
| candidates | separate_candidate_box(source_set: SourceSet, box: Box2, constraints: Sequence[Disk], policy: NumericPolicy) -> BoxFeasibility | OUT必须有有效分离证据 |
| posterior | posterior_for_feedback(source_set: SourceSet, point: Point2, feedback: MeasurementFeedback) -> ImplicitRegion | 保留严格距离与角度条件 |
| posterior | verify_pair(source_set: SourceSet, point: Point2, x: Point2, y: Point2, policy: NumericPolicy) -> PairFeasibility | 实际共同反馈与见证 |
| robust_inner | worst_diameter(source_set: SourceSet, point: Point2, config: SearchConfig, policy: NumericPolicy) -> InnerResult | 固定点的上下界 |
| robust_outer | select_second_point(source_set: SourceSet, config: SearchConfig, policy: NumericPolicy) -> Q2Result | 完整域搜索及认证等级 |
| robust_outer | movement_frontier(source_set: SourceSet, budgets_m: Sequence[float], config: SearchConfig, policy: NumericPolicy) -> tuple[Q2Result,...] | 每预算独立界限 |
| diagnostics | local_geometry(source: Point2, stations: tuple[Point2,Point2], error_model: GaussianDiagnosticConfig) -> GeometryDiagnostic | r1/r2/φ/面积近似/Fisher/GDOP |
| adapters | bearing_from_observation(obs: mock.strategy.Observation, mode: ErrorMode) -> BearingMeasurement \| None | 仅接受合法direction |
| adapters | measurements_from_jsonl(path: Path, channel: int, mode: ErrorMode) -> MeasurementBatch | HTTP、accepted、去重、阶段过滤 |
| experiments | run_offline_two_point_case(case: AnalyticCase, config: ExperimentConfig) -> ExperimentReport | 真值只给生成器和评价器 |
| plots | render_report(report: ExperimentReport, output_dir: Path, style: PlotStyle) -> tuple[Path,...] | 图从结果构造，不改数值 |

不可在adapters里自动把所有svd_deg改成1.005°半宽；ErrorMode必须保存到结果，理论1°与保守1.005°输出不可混表。

### 8.4 输出数据合同

q1 JSON至少包含原始/有效观测数量、半宽模式、区域类型、顶点及活跃约束、直径/端点、强制圆心、覆盖三态、最大Thales值、反例点、最小覆盖圆/支撑点、容差、输入hash。

q2 JSON至少包含F定义、物理参数、候选集公式版本、推荐点及保收保证、主目标名称`physical_posterior_worst_diameter`、内外层上下界、是否取得/近似、共同反馈见证、移动距离/耗时、near可能性、辅助指标口径、运行预算和截止原因。

每次输出到新run_id目录；manifest保存输入、配置、随机种子、源码版本、依赖版本和文件清单。静态解析例标`analytic`，mock例标`synthetic_mock`，不得称为官方实测。

### 8.5 依赖与复杂度汇总

采用与mock一致的Python 3.10+。普通几何依赖标准库，批量数组/展示复用NumPy，论文图使用matplotlib，测试使用pytest；认证模式增加python-flint。SciPy的线性规划只作为测试时独立核验的可选依赖，不作为无界判断的唯一依据。依赖文件位于models/q1q2，不修改mock的requirements。

| 环节 | 时间复杂度 | 说明 |
|---|---|---|
| N组观测转半平面 | O(N) | M≤3N |
| 可行性与衰退方向分类 | O(M²) | 两个简单、可验证的确定性算法 |
| 正常有界半平面交 | O(M log M) | 顶点逐原约束复查另需O(MV) |
| 退化枚举后备 | O(M³) | 不含随机性，小规模优先 |
| 凸多边形直径/Thales检查 | O(V) | 已规范化顶点 |
| Welzl最小覆盖圆 | 期望O(V)，最坏O(V³) | 固定种子，包含性复查O(V) |
| 问题1主流水线 | 正常O(N²)，退化后备O(N³)，另计Welzl最坏项 | 不夸大为整体O(N log N) |
| F边界构造/单点候选检查 | O(K²)/O(K) | 本题K为少量直线圆弧；扩展时显式计K |
| 问题2内外层优化 | 依实际分支节点数 | 见7.6，不作无依据的多项式时间承诺 |

## 9. 与mock框架的复用边界

### 9.1 直接复用

- mock.geometry.distance、bearing、angle_delta、advance用于普通输入/展示，保留东0°逆时针语义；认证核心使用等价的区间谓词，不借普通float函数给出证明。
- Action、Observation及next_action合同；如需局部两点实验，薄策略只执行给定第一点、调用选择器、执行第二点、退出。
- Backend.send/exchange与InProcMock用于合成实验，避免复制HTTP通信。
- Simulator、Scenario、Source、ErrorField用于生成观测与验证；单源解析案例可直接构造Scenario/Source，不必使用默认10—16源随机生成器。
- 原有recording和JSONL请求/响应字段；新适配器读取公开字段，不读取scenario中的源位置或ρ作决策。

### 9.2 不直接复用的算法

- strategy.intersect有abs(cross)<0.08和射线距离≤1600的baseline阈值，求的是中心射线交点；它不代表角锥交集、直径或最小圆。
- calibration.observations.clip只有已有有界多边形裁剪能力；auto_localize从±1800方形出发，还使用距离外包方形、顶点平均中心。不能用于问题1无界判定或精确覆盖结论。
- evaluator的清除比例/平均清除时间针对问题3/4整局；问题1/2新建局部指标报告，不为满足该评测器而添加无关搜索动作。

### 9.3 后端与真值隔离

离线实验中真值只用于：生成合法direction、验证实际源包含性、绘制标注为合成真值的图、计算辅助局部指标。选择器只接受第一次观测和公开物理边界。

保收信号保证不是“在若干mock种子里都收到”的经验结论；解析候选判据才是依据。mock不同地点iid、smooth、固定±1、空间±1仅用于压力对照，不穷尽连续最坏情况。

本任务不要求官方演练，更不涉及正式测试。现有框架的官方会话限制继续有效，但不构成本设计文档的执行阻碍。

## 10. 数值、退化与稳健性

### 10.1 角度与环绕

- 输入读数在接口层检验[0,360)，手工数学输入允许归一化并记录原值。
- 方向比较使用angle_delta或点积/叉积，不直接比较abs(θ1−θ2)。359.9°与0.1°相差0.2°。
- 构造角锥直接用θ±ε的三角函数，不因跨0°拆成两个角锥后误取交集。
- 统计/绘图需区间时拆成两段，数学上仍是一个圆周区间。
- 0°/360°、−0.0统一；不可把180°相反方向当成同一射线。

### 10.2 尺度与误差预算

计算前将坐标平移至检测点中心附近，再用L=max(1米、检测点跨度、相关物理半径)做无量纲缩放，结果反变换。极长角锥顶点可能远超L，谓词误差还需随参与运算的数值大小传播。

工程浮点模式的建议起点：归一化长度绝对容差1e−10、相对容差1e−12；角度判别用弧度约1e−12；平方长度/叉积/Thales量各按其量纲单独计算阈值，不把米容差直接用于平方米。

这些数值只是初始配置，须通过尺度不变性测试。数值容差不得加入ε当作新的物理误差；不能逐步“膨胀半平面直到有解”。在阈值带内升级精度，仍不明则UNRESOLVED。

### 10.3 高精度与证据

普通计算使用float64；病态行列式、贴边包含、微小Thales值、平行关系、零面积分类时，按80、160、320位等逐级提高精度。高精度重算必须从原始坐标/角度重新构造约束，不能仅把已经有误差的顶点转成高精度。

理论精确零（解析例中相同线、同角、对称关系）可利用符号来源标签识别。实际输入只有浮点数时，认证的是该输入表示及其明确舍入模型；不宣称恢复了未知真实精确坐标。

边界相切时YES/NO可能需高精度或符号见证；普通数值小正数不等于严格反例，小负数也不等于严格保证。

### 10.4 病态但非退化

记录最小有效边界交角、线性交会矩阵条件数、顶点最大尺度、可行性残差。病态时允许有限但很大的直径。禁止：abs(sinφ)<某经验阈值就报告无界；给GDOP设置任意上限后称其有限；用图窗裁掉超长区域后计算直径。

## 11. 验证方案与解析案例

本节为后续实现的验收清单，本轮不编写/运行这些测试。测试应针对独立数学事实和真实失败模式，不只复刻函数内部表达式。

### 11.1 问题1解析案例表

| 案例 | 输入/构造 | 可核验结果 |
|---|---|---|
| 单角锥 | S=(0,0)、θ=0°、ε=1° | 无界，衰退见证(1,0) |
| 同点重复 | 重复单角锥任意N次 | 区域不变，不缩小误差 |
| 两射线交点 | ε=0，S1=(−1,0)、θ1=0°；S2=(0,−1)、θ2=90° | 单点(0,0)，d=R=0 |
| 相向共线射线 | ε=0，S1=(0,0)、θ1=0°；S2=(10,0)、θ2=180° | 线段[0,10]×{0}，d=10，R=5 |
| 背向不交 | S1=(0,0)、θ1=180°；S2=(10,0)、θ2=0°，ε=1° | 空集 |
| 两点正交对称 | S1=(−a,0)、θ1=0°；S2=(0,−a)、θ2=90°，ε∈(0,45°) | 见下方公式 |
| 等边三角形 | 第3.4节三组±1°角锥 | d=L，R=L/√3，面积√3L²/4，覆盖失败 |
| 正方形 | 顶点(0,0),(L,0),(L,L),(0,L)；底边检测点(−h,0)、θ=1°，h=2L cot2°，绕中心旋转0/90/180/270° | 交集为正方形；d=√2L，R=L/√2，覆盖成立，多条最远对 |
| 三角形缩放/旋转/平移 | 对反例施加刚体变换及正比例缩放 | 覆盖真假不变，d/R随尺度同比，面积平方同比 |

正交对称例令t=tanε。顶点为

v1=(at/(1−t),at/(1−t))，v3=(−at/(1+t),−at/(1+t))，

v2=(at(1−t)/(1+t²),−at(1+t)/(1+t²))，v4为v2的坐标交换。

d=2√2 at/(1−t²)，面积=4a²t²/(1−t⁴)。最远对v1,v3，中点(at²/(1−t²),at²/(1−t²))；其直径圆包含另两点，R=d/2。该例适合a=500米、ε=1°与多种半宽，检验角锥精确面积相对条带近似的偏差。

### 11.2 几何核心单元测试

1. 东/北/西/南、359.99°、跨0°半宽、180°相反方向；每个角锥内部点和两个边界点满足正确符号。
2. ε=0的前向射线不含后向点；ε=90°是一个半平面；非法半宽被拒绝。
3. 空、点、线段、薄多边形、普通多边形、射线、带状无界、全平面分类。
4. 同向重复边界取更紧者；反向平行边界的带/线/空集分别正确。
5. 对随机小M，将增量可行性/队列交与全对边界枚举、独立线性规划可行性结果对照；不一致保存原始失败数据。
6. 卡壳与全顶点对枚举一致，尤其平行边、正方形、多个最远对、连续共线边。
7. Thales判据与独立最小圆一致，返回的反例点确实在强制圆外；等边三角形达到Jung上界。
8. 随机合法小点集的Welzl结果与全部两点/三点候选圆枚举一致；重复点、三点共线、近共线、直角/钝角三角形均覆盖。
9. 乱序观测、重复约束不改变集合；增添约束使有界直径不增加；若变空必须保持空集语义。
10. 平移、旋转、尺度变化后恢复结果；接近接口±2000000坐标不因消减误差误分类。

### 11.3 候选区域解析测试

1. S=0、θ=0的完整扇环；比较g_L/g_H的端点和圆弧极值与手算公式；包含第5.7节q=(750,400)。
2. 旋转/平移后正确考虑目标圆域：整体平移坐标时目标圆心也必须一起移动，不能只平移检测点而期待不变。
3. S在1800圆域边界、外部但可收到信号、以及离圆域太远导致F为空。
4. F中r_1>1000的点验证ρ下界随r_1提高；构造q被“全部1000米圆盘交”错误排除但实际属于C_sig的案例。
5. q=S必保收信号；F非空时C_sig不应被算空；q极远必被某个源点圆盘排除。
6. 若q不在C_sig，取分离见证p及ρ=max(1000,r_1)，验证首次direction合法而第二次无信号；若见证是开边界上确界，需找到邻近实际违反点后才能作为仿真案例。
7. C_dir在距离恰5米时区分等号取得与仅为下确界；q=S是重要反例，不能被简单闭包阈值误判。
8. 检测点可在目标圆域外；预算圆造成空集/退化、候选边界切线与圆弧极值必须覆盖。

### 11.4 最坏直径与全局优化测试

1. 在小型人工F上，直接枚举可实现读数的后验直径，与不可区分位置对公式独立对照；枚举只作收敛对照，不作为认证。
2. 两个已知点的辅助SourceSet夹具：共同direction当且仅当夹角≤2ε；共同near当且仅当两者都距q≤5；夹角阈值两侧分别测试。
3. 人工F完全落入D(q,5)：J=diam(F)≤10；完全不与该圆相交时near分支不参与。
4. F包含被第一次direction排除的5米边界邻域，验证严格约束与上确界标志；闭松弛孤立点不能生成虚假下界见证。
5. q=S返回diam(F)，同点重复不产生虚假增益。
6. 内层每个见证x,y及共同读数构成两个合法不可区分世界；下界不得超过独立高精度上界。
7. 外层每个L_B见证对在矩形多点抽查，并独立检查区间全块证据；不能用中心点最坏值给整块下界。
8. 候选矩形中心不可行但边缘可行时不得漏删；窄候选区域、最优点在边界、多个对称最优点均保留。
9. 收紧容差时上下界嵌套或在明确重算原因下解释变化；迭代已得到的最优上界不增加、下界不减少。
10. V(B)不增；对称情形上下侧候选有相同主值与移动距离，不强行称唯一解。
11. 正交启发、固定侧移200米、前进后侧移、完整域最坏直径优化作对比；主方法须保留真实目标，不以90°代替。
12. 达到节点/时间上限时输出有效gap与UNRESOLVED状态，不输出未经证明的global_optimal=true。

### 11.5 mock局部验证

- 手工构造合法全向Source/Scenario，选择真实源在F的中心、角边界附近、r_1≈5、1000、1500和目标域边缘。
- 对iid、smooth、恒±1°、空间±1°分别生成方向；理论1°例从无舍入解析输入进入，mock两位返回使用配置好的误差模式。
- 原始Observation与JSONL转换后喂入solve_q1，验证真实源在角锥区域内；若用了1°而舍入超出该界，按已知模式差异报告，不把它归因于几何算法。
- 理论的连续读数模型与两位小数接口区分：1.005°模式是对潜在误差加舍入的保守外包，认证的是这个外包模型，不能宣称恰好等于官方量化观测的真实最坏值。若后续加入已知舍入规则的精确量化模式，共同direction条件还必须要求两个允许读数区间的交中存在0.01°网格值；仅有连续区间相交不再充分。该精确量化模式不属于本版默认实现。
- 选点器只能看到第一次观测；核对C_sig保证点在全部合成验证世界收到direction或near。有限样本不替代证明。
- 近距离反馈只映射near，不填假示向度；无效HTTP、accepted=false、重复request_id、清除后测量不得污染输入。
- mock实测耗时与q移动距离/5+5核对；不把内层优化耗时计入虚拟移动时间。

## 12. 论文图表与输出验收

### 12.1 必需图件

| 编号 | 内容 | 必须标注 |
|---|---|---|
| F1 | 任意N点的角锥交会定位区域 | 检测点、中心射线、±ε边界、顶点、坐标单位 |
| F2 | 正交对称例的区域、直径、强制圆、最小覆盖圆 | d、R、最远端点、覆盖成立；圆重合需图例说明 |
| F3 | 三角锥形成等边三角形的反例，全局图+局部放大 | 3个读数、真实源O、反例顶点、d/2与R、Thales正值 |
| F4 | 无界、空集、线段、近乎平行四类示意 | 无界箭头与“绘图范围非约束”，不伪造有限直径 |
| F5 | 问题2首次F、完整C_sig及C_dir | 1000/1500、5米排除边界、目标圆、允许域外检测、推荐点 |
| F6 | 完整候选域上J(q)的热力图 | 真实最坏物理后验直径、界限/gap；不可行与未认证区不同填色 |
| F7 | 主方案最不利的不可区分位置对与共同反馈 | x/y两个合法世界、共同示向度或near、后验区域与直径见证 |
| F8 | r_2与φ的联合效果：面积与GDOP双面板 | 固定哪一个源位置/误差模型；显示r1·r2/|sinφ|；90°仅条件最优 |
| F9 | 定位效果—移动成本折中曲线V(B) | 预算B、实际移动、上下界带、耗时换算 |
| F10 | 不同策略的直径/面积/GDOP比较 | 最坏直径与条件面积口径分开，合成数据标签 |
| F11 | ε=1°与1.005°敏感性，及物理后验与纯角锥结果对照 | 不同模型字段、无界对照、舍入假设 |

图F6网格只是可视化，每个像素可用内层近似并标精度；不能据图上颜色最低点宣称全局最优。候选集外不得填成0直径；无界GDOP使用专门符号/遮罩，不饱和成某个有限值而不注明。

### 12.2 必需表格

- T1：题面事实、假设、待审口径及来源表。
- T2：第11.1节解析构型，N、区域类型、顶点数、面积、d、R、覆盖真假、解析误差。
- T3：问题1算法复杂度、退化后备、数值状态与验证残差。
- T4：问题2各输入场景的推荐点、是否保收/保direction、最坏直径上下界、移动距离、耗时、最优性gap。
- T5：启发式与完整域优化对比；无信号/near频数仅为合成实验统计，明确不是概率保证。
- T6：数值精度、节点数、运行耗时、停止原因；未达标结果原样列出。

### 12.3 绘图与文件标准

坐标图等比例，单位米；中文字体显式选择并记录；矢量PDF供论文，PNG供预览，必要的数值CSV/JSON一起保存。几何边界与不确定边界带区分线型；所有图从保存结果重绘，不手工移动顶点“美化”计算结论。

每张图的说明包括输入案例、半宽模式、纯角锥/物理后验口径、是否合成真值、认证级别。论文可展示代表性精确例，同时必须说明一般优化案例的数值gap。

## 13. 模糊点、假设与待审清单

### 13.1 已由材料明确，无需再询问

- 方位是检测点指向源，东0°逆时针；不是北0°顺时针，也不是源指向检测点。
- 所有源在1800米圆域，但检测点可在域外。
- 有效接收半径是每源固定未知值，不是每次测量随机变化。
- 5米内返回near，20米内可光学定位清除；两者不是同一个阈值。
- 同点重测不改变当地误差；题面未保证不同点独立。
- 问题1没有给数值数据集，所以需算法与可解析验证实例；不能编造“题目提供的测量表”。

### 13.2 已确认并固定在本稿的建模选择

- 问题1仅纯角锥，不加物理圆域约束。
- 最坏直径主指标；面积和GDOP辅助；移动次级。
- 主候选区域对全部一致世界保证收信号，允许near。
- 理论1°；接口保守1.005°，可配置。
- 使用任一最远对的强制圆心定理；提供真实角锥反例。
- 不建立未经资料支持的位置概率先验；不把mock默认均匀源分布当题设。

### 13.3 本稿新增、供用户本次审计决定的口径

1. **问题2后验集合：**本稿主目标为完整物理后验K的最坏直径，J_ang为纯角锥对照。这是最大的一项新增细化，详见0.4、6.1与6.8。
2. **near的主评价：**测量后采用F∩D(q,5)的真实直径，≤10米；不把“随后可调用光学设备”直接当作本次反馈后直径0。若需评价“测量+光学操作”的终端定位，应另加动作并改目标与成本。
3. **严格全局最优与工程预算：**设计提供验证上下界和ε最优合同；退化/资源不足时允许返回未达标gap，不能承诺所有输入在固定时间内认证完成。
4. **问题2主优化的角度支持：**主物理设置为1°/1.005°；多项式角判据支持0<ε_2<45°。问题1的更广角度支持不自动扩张到问题2优化器。

以上是审计点，不授权实施agent自行换口径。未经修改本文，按当前明确默认值实现。

### 13.4 附件仍未明确，需保留不确定性的事实

- ±1°是否包含两位小数舍入，以及具体舍入方式；本设计不声称已知官方规则。
- 不同地点误差的分布与空间相关性；无法从题面给出统计置信区间或独立同分布保证。
- “较好的定位效果”没有指定唯一标量指标和移动预算；已按用户决策具体化，不声称这是题面唯一解释。
- 题面未给障碍物、禁行区、源位置先验概率或最小移动距离，因此默认没有；不自行添加固定侧移200米等限制。

## 14. 后续实现顺序与完成标准

1. 实现类型、角度和数值策略；确认区间后端与认证标签规则。
2. 完成问题1分类、半平面交、卡壳、强制圆心覆盖和Welzl；通过所有解析例与独立交叉验证。
3. 完成F边界与C_sig/C_dir的解析成员/分离判据；先验证完整候选集，没有该步骤不进入策略优化。
4. 实现反馈后验与不可区分位置对验证，再做固定q的内层上下界。
5. 实现完整候选域外层搜索、移动次级择优和预算曲线；明确未决结果。
6. 接入mock观测与合成局部实验，验证真值隔离及舍入模式。
7. 从结果生成论文图表、数据清单与复现说明。

完成标准不是“能画出像图2的四边形”，而是：任意N输入被正确分类；覆盖结论有可复查证据；真实反例成立；候选域没有被启发式截断；最坏直径目标直接包含距离与夹角；数值最优性声明与实际上下界相符；每一张论文图都能追溯到明确输入与模型口径。
