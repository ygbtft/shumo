本轮找到一套不增加站数的合法非圆形 21 站布局，两个独立验证种子块均有小幅收益；没有取得接近地板的结构性突破。主候选默认保持原样，获得独立验证收益的实验候选为 `noncircular_731_0`。

普通混合场合并 840 个独立场景：源加权 τ **471.13918 → 467.31925 秒/源，下降 0.811%**。再加入 210 个压力场景：**485.27249 → 481.22497，下降 0.834%**。这些基线是本轮固定种子矩阵的实际结果，不能把其他矩阵的 449—468 与本轮候选直接相减。

共 4214 次本地 mock 执行，全部全清。合并验证 586 局更快、464 局更慢；最大单局回退 **+2803.582 秒**。因此保留实验注册，不建议据此替换默认。没有找到合法 <21 站布局，也没有证明任意 20 站不可能或当前 21 站巡回近全局最优。

**做了哪些结构搜索。** 所有几何输入都来自抽象源圆盘和固定坐标族，未用 mock 真源位置优化布局。

|搜索|规模|结果与边界|
|---|---:|---|
|方向半圆盘覆盖整数规划|416 个位置；站数上限 20、21；分别 2、11 次求解|允许多站同时替换、混合不同环层和方格位置。反例分离拒绝漏覆盖解；最后均超时且没有新 incumbent。不是不可行证明。|
|选址＋开放巡访＋服务近似的联合 MILP|105 个扰动位置、18 轮；129 个混合位置、12 轮|后者允许各站独立选不同合法轮廓，从而产生非对称布局。加入可选点子环约束和方向反例；两个无采样盲区结果仍未获精确证书。无候选进入 mock。|
|非圆形 21 站族|可行性探索 1050 次目标计算；联合近似 10700 次|内层轴向/斜向点、外层轴向/两种斜向坐标独立变化。16 次证书尝试有 8 次通过，其中 2 个是基线点集重复；6 套非基线布局注册为实验。|
|去中心的非圆形 20 站族|14600 次目标计算|未找到采样可行解；保存最佳失败布局及方向反例。只否定该具体候选，不把有限搜索说成全族证明。|

计数是求解轮次/目标计算次数，不是声称穷尽了相应连续空间。完整池、候选坐标、反例、状态和界保存在 `cover_*`、`joint_*`、`noncircular_*` 子目录。

联合筛选使用 `L/5 + 42K + S_proxy`：42K 近似源数均衡时 7 个空频道的测量与切换，S_proxy 对抽象方向探针分配最近可接收站，计入接近距离和角宽服务近似。它不是精确时间，也不是任意布局下界；N=16 提前终止和动态服务绕行必须由完整 mock 决定。最后按真实 `L/5 + 5M + 切频 + 5清成功 + 3清失败` 计时。服务近似领先的两套获证布局在扩大训练中反而退化，未因代理分数好看而推广。

**获证的短巡回布局。** 内层为 `(±1046,0),(0,±1046),(±665,±665)`；外层为 `(±1867,0),(0,±1867),(±1609,±943),(±943,±1609)`，另有原点。内层不是单一半径的规则环，轴向点约 1046m，斜向点约 940.45m。

固定点集的最短开放巡访：**17826.74066 → 17737.39302 m**，缩短 89.34764 m（0.501%）。这是固定站集上的 MILP 数值最优，从原点出发、不要求返回；不是所有合法布局的全局最优。闭环仍使用原有动态调度器。

[整数坐标](selected_coordinates.csv)；[访问顺序](selected_route.csv)；[实验工厂](registry.py)；[注册表](candidate_registry.json)。未计分的注册布局明确标为 `mock_evaluated=false`，不能视为获得了闭环收益证据。

![布局与固定站集开放巡访](layout_comparison.png)

**训练与独立验证。** τ 为各局总虚拟时间之和除以各局真实源数之和。普通场景覆盖 10—16 源、均匀/边缘/聚簇、五类有界误差、普通半径方向与固定 1000m 朝外模式；压力场另含圆周朝外/切向、接收过渡带、近共线、中心簇加远频道20。

|阶段|普通/压力场景数|普通场基线→候选 τ|含压力场基线→候选 τ|
|---|---:|---:|---:|
|训练初筛|105/105|456.31617 → 454.03922|497.04662 → 494.16756|
|扩大训练|210/105|468.90824 → 468.40361|491.74067 → 492.39167|
|独立验证 A|420/105|470.34424 → 466.27022|484.03338 → 480.36107|
|独立验证 B|420/105|471.93412 → 468.36828|486.51160 → 482.08887|
|两组验证合并|840/210|471.13918 → 467.31925|485.27249 → 481.22497|

训练种子起点为 206091300、206092000；验证 A/B 为 210000000、210100000。完整场景/误差种子见各阶段 `fixtures.json`，最终审计验证训练与验证种子集合不相交。验证前冻结坐标、顺序和策略参数；第二组用于检查训练压力场反转，不调整候选，两组无论正负均报告。

合并普通场 Δτ 的配对场景 bootstrap 95% 区间为 **[-6.174, -1.529] 秒/源**；含压力场为 **[-6.270, -1.969]**。这是本地人工矩阵的抽样区间，不是官方分布或逐局改进保证。训练初筛和扩大训练的压力结果不一致，均原样保留。

验证 A 另按相同种子完整重跑 7 个普通场和全部 105 个压力场，两策略合计 224 次，时间、距离、测量、切换、清除结果逐项精确复现。它们只计入执行次数，不作为额外独立样本。另有 16 次恢复公开请求 UUID 的逐条协议回放，保留请求全部字段进行比较。

**收益来自哪里。** 下表为两个验证块合并、包含压力场，单位秒/源；服务移动是非巡查动作的位移，定向服务移动是其子集。

|分项|基线|候选|变化|
|---|---:|---:|---:|
|巡查动作移动|234.67086|231.09936|-3.57150|
|服务动作移动|114.25218|114.07220|-0.17998|
|其中：定向源服务移动|76.76874|76.34023|-0.42851|
|测量/切换/清除|136.34945|136.05341|-0.29604|

主要收益仍在巡查移动；定向成对探测的大开销基本保留。站数仍为21，没有新增空频道固定扫描成本。该结果只削掉约4秒/源，没有填平相对196秒/源地板的250+秒差距。本轮没有重试150，也不重算或改写已知地板证明。

![验证中的成本变化与逐局回退](performance.png)

**逐局回退。** 全部 1050 个验证配对（含收益局）见 [排序表](validation-pooled/regressions_sorted.csv)。最差为 `210124160`、16源、`cluster_far20/positive`：基线 3859.758074 秒，候选 6663.339582 秒，增加 2803.581508 秒。轨迹显示基线访问11站后达到公开16源上限，候选访问了21站。两者均全清；没有用真值选择每局较快布局，也没有把事后取两者最小值当成可部署回退。回滚方式是直接继续使用未修改的主候选。

**硬约束与复核。** 新布局先通过原版 `B/integer_visibility_certificate.py` 的生成器及 Python 大整数独立验证，再通过严格浮点凸包复核、原版 `B/icra_final_checks.py:partition_check`。完整根方形显式补齐圆外叶格，逐格用整数距离证明其与半径1800源域不相交，用精确有理面积验证铺满3600²；没有把“仅落入1000m圆盘”当作Q4任意180°朝向保证。这里覆盖源圆盘，并完整分区根方形；不宣称源圆盘之外的根方形四角也有接收覆盖。

最终重新从磁盘读取全部9份通过证书，检查坐标与路线集合一致，并运行删接收叶格、删圆外叶格、改接收半径1001m等负对照。原有 `tests/test_visibility_certificate.py` 的11项测试通过。只调用 ICRA 原函数进行本轮证据检查，没有调用会写历史清单的 `icra_final_checks.main()`。

最终审计：**4214/4214 全清，0 认证清除失败、0 定位区域真源排除、0 排尽性失败、0 接口拒绝**。源位置仅在策略结束后用于计分和包含性审计，策略只接收公开协议反馈。所有受保护 B/ 与 mock 顶层源码及原布局共 193 个文件哈希未变；20m、±1.01°、1000m保证和默认候选均未改。全程使用指定 Python 和本地内存 mock，没有调用官方模拟器、正式测试或读取 `jlog-research/` 恢复数据。

[最终检查](final_checks.json)；[分阶段统计及置信区间](paired_analysis.json)；[逐请求轨迹目录](validation/traces/)；[原测试结果](original-coverage-tests.log)。

复核现有证据：

```sh
/Users/flower/math/2026/B题/mock/.venv/bin/python /Users/flower/math/2026/B题/review-opt/q4-joint-layout/audit.py
```

重新跑验证 A（使用新目录名，拒绝覆盖原证据）：

```sh
/Users/flower/math/2026/B题/mock/.venv/bin/python /Users/flower/math/2026/B题/review-opt/q4-joint-layout/benchmark.py --phase replay-a-new --count 420 --seed 210000000 --candidates noncircular_731_0 --workers 3
```

搜索脚本的早期模式已保留为命令行选项：731 使用 `--objective feasibility --no-exact-gap`，732 使用 `--objective joint --no-exact-gap`，733 使用 `--count 20 --objective joint`。重跑需加新的 `--tag`。计时停止的优化器不保证重新枚举相同候选数量；交付坐标、证书和逐局计分可独立复核。
