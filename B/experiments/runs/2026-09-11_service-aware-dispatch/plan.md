# 下一阶段：计入扫描费用的有向任务规划

上一59514计分与293项归档哈希已核验，上一goal轮属于progress。仅B/写入，CPU单线程，基础seed42，原件/上游只读，正式请求0。

现有任务规划主要使用扫描点、下一探测入口与可行区域中心之间的路程。新增有限视窗的静态替代目标：路程+尚未清除且不能按当前区域排除的已知源扫描费用，以及可选的原扫描次序偏离代价。已能保证光学清除的源原策略已经跳过RF，相关扫描费用必须为0，不能重复奖励。费用权重只作策略排名，不当作未来示向或真实源数。

实现自由有向2-opt、保持扫描次序的有向插入、允许有限偏离的软次序三类调度。0扫描费用/0次序费用应与已有有向路线严格一致。区域更新、Q4成对探测、生命周期RF与暂停预算、全域覆盖、光学兜底、停止条件保持已有保证。

先检查路线目标增量与穷举成本一致、零权重控制及全部旧压力的公开协议；再冻结训练配置，在已使用67—71与旧压力上比较。保存全部正负结果和CPU，不以单个均值选“全面最优”。训练后再确定少量候选；157—166及新压力流必须在新候选冻结后生成，本目录训练不会读取它们。

本轮重点是检验扫描与源服务联动是否减少既有千秒回退。预期收益是假说，尚未运行或证明时间支配。下一轮报告继续明确同学缺完整源码、截图缺同口径数据、官方验证未进行。
