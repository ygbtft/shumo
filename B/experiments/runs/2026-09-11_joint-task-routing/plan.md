# 联合任务路线训练计划

2026-09-11。前一轮1767/1767全清，Q3共享测向改善均值但仍约316秒/源；Q4成对探测降低压力尾部但普通约671秒/源，仍未达到240/460目标。移动占总耗时约75%，下一步把未来覆盖站点与已发现源的可行集中心放入同一条反馈驱动任务路线。

1. 独立新增任务调度策略，比较贪心、动态2-opt、定位移动权重、是否共享测向；Q3继续外包角锥/MEC及合法第二点，Q4继续成对探测和有限光学兜底。
2. 路线中的可行集中心只是预测处理位置，不能当成真实坐标或清除保证；实际清除仍检查MEC或承认一次试探失败并保留区域。
3. 原有Q3九点圆域覆盖、Q4二十五点方向覆盖站点全部保留。允许跳过已发现且精度足够的频道；剩余未知频道在每个保证站点仍须检测。达到公开上限16或完成覆盖并清除全部已发现源后结束。
4. 用67—71的75普通训练场景和原18压力场景比较；这些已用于调参。77—86仍未查看。先冻结代码/配置，再后台单线程CPU运行；基础seed42派生。全部输出B/，不改上游/原件，无正式请求。
5. 保存配对逐例均值、P95、最大、每源、指令、移动、CPU/墙钟、变慢案例、失败和完整协议轨迹；检查重放不依赖真值。

运行命令见command.sh。预检查先验证新调度在完整协议的压力案例及共享检测有限性；通过后才正式计入本轮执行数。
