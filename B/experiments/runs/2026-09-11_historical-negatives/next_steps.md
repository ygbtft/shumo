# 后续与训练选择

1302计分全清，665前置检查通过。Q3历史候选无虚拟收益且CPU增加；不晋级。Q4 first8作为普通/压力和CPU折中，recent8作为训练压力较好候选晋级127—136；历史arc训练压力变慢，保留而不晋级。确认已完成，详见history-confirmation。

最终所有449个本训练有效历史收缩执行逐更新保留真实源，属于审计不增加成绩。历史first/recent的新确认普通改进区间均包含零，且CPU、逐例回退尚未解决；不设默认。可尝试在不改变裁剪结果的前提下对同一几何点缓存/筛选，先严格反馈等价，再测CPU。不能仅凭区域更小宣称整局更快。
