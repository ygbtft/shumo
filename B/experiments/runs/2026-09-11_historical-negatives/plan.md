# 历史成对无信号策略训练

独立实现一个成功锚点和两个实际负反馈的五半平面排除集合，按最近4/8/16个负点及首次/最近/两端成功点消融。保留原接收范围证书跳过、简单分包与加速服务/固定站序作为参照；未知频道不按先验停止，原有限光学与累计预算不变。

先用独立线段相交与距离比较检查半平面等价，测试原窄对可截断、45度不满足距离前提的真实反例、随机合法半圆/圆盘接收和共线退化；再做关闭功能的完整反馈重放与完整压力协议。只在已见67—71和旧压力训练选参，新127—136及新压力流留待冻结确认。

基础seed42、CPU单线程、仅B/写入、正式请求0；几何覆盖搜索不是计分全清执行。保留不一致/失败/回退和CPU开销，不因更多区域裁剪就宣布整局更优。
