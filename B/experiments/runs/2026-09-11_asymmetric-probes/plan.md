# 公开数量上限与非对称成对探测

2026-09-11，goal延续。上一轮为实质进展：1950新场景确认、22点覆盖、报告与检查已落盘；目标尚未完成。全部仅B/写入、本地单线程CPU、基础seed42，禁止正式调用。

1. 去掉replacement22中没有实际触发的凸包替代计算，单独实现已发现16个唯一频道后停止发现阶段。用原195条记录做行为等价重放（仅机制核查，不计新场景成绩）。
2. 分析Q4二十二点相对二十五点最差的已知回退轨迹。原先所有源都以可行集中心预测处理位置，首次前向探测采用中点，可能对近处源造成过冲。成对探测的几何证明适用于任意正l，并不要求二分。
3. 比较首步/每步不同分位、逐步增长的探测距离、任务位置预测与光学试探门槛；所有实际清除仍用原角锥/MEC、合法成对无信号排除和固定预算后的有限光学兜底。非对称选择只影响效率，不能假设源距离已知。保持参数和预算有界。
4. 在已用67—71普通训练和旧压力场景选择；上一轮已知回退作为明确复用的回归集。87—96及新压力流尚未查看，冻结候选后另开确认目录。
5. 分别保留全清、虚拟总/每源、P95与最大、指令、实际CPU/墙钟、失败/变慢轨迹；继续以240/460的固定平均每源口径及压力尾部为目标，不能换加权口径报喜。

阶段命令见command.sh，后续配置在运行前冻结。原件、公共探索和上游只读。
