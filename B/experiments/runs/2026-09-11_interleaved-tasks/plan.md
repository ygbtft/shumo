# 探测后重排联合任务

上一goal回合分类：progress。新增68项核查、更新22554执行归档，并由已见Q4 uniform/iid/93两条公开反馈轨迹查明：局部源任务省652.76s，但扫描访问7变17个、多2183.32s。此证据改变下一步，不是状态复述。

本轮实现探测包：Q3一次测向，Q4一组不可拆开的成对探测，然后重新选择扫描/源任务。每源探测轮数跨任务累计，Q3最多3次主动RF、Q4最多10轮20次；提前光学试探至多一次，原有限光学兜底不变。比较MEC中心预测、下一实际动作预测、贪心最近任务及2-opt，保留7/9点Q3和22/25点Q4布局的连续覆盖保证。

为避免重排导致最坏移动界越过100小时，整局最多24次主动打断未完成源；预算耗尽必须继续该源至完成。相对原264096s宽松界，每次打断按额外两个跨域转移2×5200m/5=2080s计，因此新界≤314016s<360000s，指令界仍9766。该额外量保守覆盖离开及回到未完成源的移动，原每源内部动作预算不增加；不把旧1900m内部转移界套给任意跨源返回。

13候选×93个已见训练场景=1209次。普通seed42+25+repeat（67—71），旧压力流完整保留；97—106不生成。先做零打断与旧控制逐请求等价核查、完整压力协议及累计预算检查，再冻结代码和参数后台CPU单线程运行。全部清除优先，保存所有失败和逐例变慢；不按未见数据改参。不注册、不联系现有HTTP服务或使用正式机会，所有写B/。
