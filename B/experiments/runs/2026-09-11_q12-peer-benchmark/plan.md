# 用户指定的Q1/Q2 benchmark复核

用户要求暂停此前持续优化，先测试b分支HANDOFF中的Q1/Q2。优化后台均已结束并保留结果，不启动新的Q3/Q4实验。本任务仅离线、只写B/。

上游独立副本提交253bf943a58d6f21de88f14fdc940da66b87ab96，保存在B/reference/shumo-b-q12-benchmark，之后只读。完整HANDOFF已读取；原始四组case.jsonl共285/330/531/48例及原报告均已保存/登记SHA256。固定回归夹具保留上游种子，不重生成或按结果修改；新增独立随机检查用42。

1. 在本目录upstream_worktree中复制models代码，运行原版单元测试和四组标准benchmark。依赖沿用指定Anaconda Python；禁止pyc、pytest插件自动加载，缓存及临时文件全部放B/。
2. 保存每条命令、退出码、墙钟和完整stdout；benchmark退出1属于待分析的检查失败，不停止其余独立组。检查源代码和cases是否被运行修改。
3. 将我们的已有Q1/Q2函数接入同一独立真值输入；转换接口只做表示适配，不借用同学生产算法补答案。对无法匹配的接口和数学口径明确记为未覆盖，不虚报通过。
4. 抽查失败的expected/actual：区分答案/分类错误、输出规范化、保守拒绝、尺度边界和benchmark自身要求；不直接沿用HANDOFF“残留全部无害”的结论。
5. 对Q2连续最坏量补充有预算的区间认证或解析案例；未收敛区间不当精度通过。保存对照表、失败输入、复核结论和当前代码限制，更新B/REPORT.md及本目录summary/next_steps。

正式/演练官方接口均不调用，不注册或使用身份。不存在Q3/Q4成绩增量；本次是独立正确性测试。
