# 根据首轮盲区检验较小内核

首轮216个组合没有通过方向覆盖，保留全部实际反例而不将它们描述成单纯认证器失败。部分盲区位于约900m处：较大内核的另一个点也超过1000m接收范围。仅据这种几何证据把内核缩为50—200m、中层1070—1100m，保持中外层15/45度错相。192个组合仍需全域连续证书，不据无反例的有限采样直接接受。

所有候选外层≤1900m，不扩大既有动作界。基本seed42、CPU单线程、只写B/、正式请求0。这是几何设计检查，不计策略执行。与第一轮的联系及适应性选择必须在报告中明确。
