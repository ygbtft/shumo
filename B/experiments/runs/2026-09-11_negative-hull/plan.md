# 负反馈排除锥实验

在1674次任务联动训练后，独立推导凸接收集合的负反馈约束并实现。位置g和两成功接收点a,b组成的三角形内一定能接收；实际无信号点q因此排除q+cone(q-a,q-b)内的g。用补半平面并的凸包保守表示，保留所有覆盖和有限预算。

先独立验证解析夹具、真实半圆/圆盘接收场景及退化射线，做关闭功能的完整反馈控制重放，再比较开启负反馈约束对原范围跳过、固定扫描顺序、服务入口/出口调度的效果。只用已见67—71和原压力训练，117—126留待冻结确认；基础seed42、CPU、只写B/、正式请求0。全部负结果和不一致计数必须保留，不能因区域收缩就认定耗时下降。
