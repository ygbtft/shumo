# 21站整数布局与扫描优化的冻结确认

1116次lean、744次公开16源、1860次布局训练，共3720计分全清。grid21_29在训练中较22站兼有普通/压力均值、尾部、指令及多数CPU指标改善；晋级简单距离、预测、固定站序和公开16源版本，并保留各自无删除参考。grid21_7作为普通均值备选，closed21_3作为闭边界敏感性对照；两者训练压力尾部较差，负结果保留。Q3公开16源训练无额外删除，不晋级；lean与原版的相同决策CPU对照保留。

冻结20候选后生成147—156=42+105+repeat及SeedSequence([42,120,problem,layout_index,count])压力，误差42+3200+100*problem+index，共3900计分。不依确认改参数。157—166及新压力流仍未生成。

全清/结束优先，普通每源算术均值240/460目标不变；P95、最大、逐例回退、指令、CPU/墙钟分别核对。21站保证来自实际整数坐标的连续证书；closed21仅在闭180°口径下解释相切点，主候选grid21_29有严格内含余量。所有新策略只读规定反馈，原件上游只读，CPU单线程、基础seed42、所有输出B/，正式请求0。
