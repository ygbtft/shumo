# 审计外层脚本的收尾错误

Python运行审计已完成：final_checks.json与完整日志均记录99通过、0失败、7620计分行、3900轨迹物理检查、2871删除展开与948等价配对。其后外层zsh退出1，错误为第19行附近的双分号解析错误，原文保存在launcher_error.txt。

当时在Python审计子进程运行期间，为同一个command.sh追加了diagnose/report-support/figure分支。当前文件单独zsh -n验证通过，错误发生于等待子进程返回后的解析阶段；依据这一时序与现象，原因判断为运行中的脚本被编辑、改变了后续读取位置。它不影响已完成Python审计、策略代码或计分数据。没有把外层失败悄悄改成成功，也没有重跑或覆盖成绩。

修正：所有Python分支以exec替换外层shell，避免Python返回后继续读取已编辑文件。当前无在途脚本后才应用这项修正。另执行zsh -n，并在最终归档调用验证正常退出；不为了外层脚本错误重复已经通过的数学/协议审计。今后先补齐命令再启动，运行中保持启动脚本不变。
