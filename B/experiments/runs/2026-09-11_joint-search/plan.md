# 持续优化 goal：联合布点、测向和清除调度

用户明确要求建立goal并持续尝试算法，使效果全面好于同学一论文实现方式；同时确认没有其源码。当前可复现基线是同学一9点1300m布点+我们的稳健定位清除；它有完整覆盖/结束保证，不能称为原论文完整程序。

本轮优先研究动作成本而非继续只换TSP求解器：动态贪心/2-opt、延迟集中清除、沿后续扫描路的机会清除、低成本光学尝试、信息矩阵近似选点。协方差只用于候选排序，真实包含集合/保收证书/MEC/光学有限覆盖仍决定正确性。

所有候选先固定；普通训练场景沿用已读的67—71种子×15条件，压力18局，共93个配对场景。不能把训练最优当成全面改进。筛选后冻结候选，再用77—86等尚未查看场景确认，并单独记录平均、P95、最慢、指令和真实耗时及逐局退步。保证全清第一优先，不用加权分掩盖失败。

写入仅B/；CPU单线程；基础seed42及派生流记录；先保存命令再后台运行。原图/题面/上游/旧策略不改。正式请求0。
