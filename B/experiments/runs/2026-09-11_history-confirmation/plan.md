# 新历史约束与保序删除的冻结确认

先通过1302次历史负反馈、1116次保序扫描删除训练；选历史first8（均值与CPU折中）和recent8（训练压力均值最佳）进入确认，保留四对无跳过/保序删除策略及已有range/分包/论文布局代理，共15方法×195=2925。Q3历史策略无虚拟收益且CPU上升、历史arc压力变慢，保留训练原始结果但不晋级。

使用未生成127—136（42+85+repeat）和SeedSequence([42,118,problem,layout_index,count])压力；误差42+2600+100*problem+index。冻结代码、参数、保证证书后才生成真值，评测只给Client。普通算术每局每源目标240/460，不换加权口径；全清优先，逐例、尾部、指令、CPU均报告。

408个21站多内核候选全部有实际盲区见证，不进入策略；保留22站连续证书。官方请求0，CPU单线程，只写B/。确认后完整物理、区域、历史有效裁剪和展开原计划审计；本地自有HTTP及CLI不计分。
