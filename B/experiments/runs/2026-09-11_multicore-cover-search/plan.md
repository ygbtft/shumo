# 非单中心21点方向覆盖搜索

上一轮为progress，当前38373次计分归档已完成。本轮尝试3个内核点、6个中层点、12个外层点的21站布局，替代单中心7+14的22站结构。不能靠有限网格通过就宣布全覆盖。

从内核半径350m、相位30度，中层半径1060m、相位0度，外层半径1868m、相位0度开始；外层不超过1950m，以保留既有移动位置界。检查1000m附近、中层顶点外侧、层间盲环和边界朝外，再搜索半径/相位组合。若得到候选，必须用自适应方格证明整个1800m圆域上、1000m最小半径的所有180度朝向均有有效站点，并独立核对方格分区与凸包/距离条件。失败见证及未解决单元保留。

本目录仅几何搜索/证书，不计策略全清执行。只有通过连续证书的布局才可进入后续策略训练；无论是否成功，都保存参数、代码快照、命令、日志、结果与下一步。CPU、基础seed42、仅B/写入、正式请求0。
