# 宽成对探测：几何约束与实际时间比较

前一goal回合分类为progress：实现分包和发现奖励，4935计分执行、65项独立核查、原采集异常记录及报告均已完成。Q4普通512.11仍未达460，近共线测向是值得验证的下一机制。

先推导q±=(l,±kl)的充分条件t≤k且k²+2kt≤1，再独立检查距离和接收半平面边界。构造45°合法方向源反例，说明任意拉宽会错误利用双无信号。实现半角3/5/10/15/20/30°，固定、首步宽、递减及两个纵向分位变体；射频读数误差外包始终1.01°。

新动作域半径3535m、连续大部分源内转移1900m、最终兜底圆心单列2500m；最多24次打断后统一上界335136s<100小时，指令9766。证明在B/WIDE_PROBE_GUARANTEE.md。所有策略保留原连续覆盖和有限清除，不拿宽角度或有限样本替代包含证明。

18候选×93已见Q4训练场景=1674次，普通67—71=seed42+25+repeat，旧压力流[42,20,...]。107—116及新的压力流不生成，先控制重放/压力检查、再冻结后台CPU单线程比较。完整保存全清、总/每源时间、P95/最大、指令、CPU/墙钟、失败/回退。仅B/写入，原件/上游只读，正式请求0。
