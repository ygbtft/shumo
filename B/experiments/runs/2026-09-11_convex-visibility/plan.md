# 直接方向覆盖证书与更少扫描点

2026-09-11。联合任务路线训练1488/1488全清，Q3七点普通258.14秒/源；Q4二十五点普通557.82、压力682.52，仍未达到挑战目标。继续检查Q4布局的充分条件是否过于保守。

题设180°闭半平面下，位置g对任何方向都可被发现，当且仅当g属于所有距g不超过1000m的站点的凸包。改用自适应方格证书：对每个覆盖源圆域的小矩形，选择距整个矩形都不超过1000m的站点；证明整个矩形包含于这些站点凸包。所有矩形通过则给出连续全域保证。无法证明就细分；达到最细尺度仍不满足则拒绝候选，不能用采样通过代替证明。

先以公开几何参数探索双层错相极坐标布局（6—9内点、10—18外点）并保留真正朝向反例，再为通过者保存全部方格证书。排列路线用已有2-opt，按点数、静态长度、证书余量筛选；通过后才进入全流程训练。所有随机流seed42，此处布局搜索本身是确定枚举。场景仍67—71和旧压力；77—86未使用。全部写B/，CPU，不改上游、不调用正式服务。

检查和运行命令保存command.sh。实际证书、搜索参数、失败见证、运行时间均记录；不能将更少站点直接等同于整局更好。

搜索后追加（正式策略比较前）：160个确定候选中11个取得全域方格证书，最少22点（7内+14外+中心），静态路线17716.55m。为减少局中冗余，增加“替代一个未来站点才做额外全频道扫描”：假设在已到达清除点扫描，若其与历史完整扫描、剩余站点共同覆盖所有证书方格，则实际扫描未知频道后再删除未来站点。覆盖只用于删除，不能靠采样或未执行的点结束。Q3使用既有连续圆域覆盖证书；Q4用方格凸包的角间隙判据。已发现16个不同频道时也可停止发现阶段，但仍须清除所有已知源。
