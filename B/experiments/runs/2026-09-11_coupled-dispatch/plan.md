# 保证扫描与定位服务联动训练

上一goal轮分类为progress：当前32889计分归档、5400新增全清、65运行审计和7条公开反馈回退诊断均已核对。本轮保持完整goal，不把均值小幅改善当全面达成。仅写B/、CPU单线程、基础seed42、正式请求0。

实现两种联动：固定原保证站点的相对顺序，已知源任务用最小增量插入；或把源任务区分进入点（下一探测）与完成点（MEC中心代理），以非对称转移代价做滚动路线规划。MEC不是源真值，入口/出口模型也不是必然实现的运动轨迹。另实现扫描时已知源的严格无接收证书：检测位置到MEC中心距离大于1500+MEC半径+余量才跳过；未知频道不跳过，不把跳过请求写成服务端回复。

先检查有向2-opt反转代价、最小插入代价、圆域距离证书、零改动与已有反馈逐请求等价，再在已见67—71及原18压力场景训练。新117—126及新的压力流不得提前生成。参数选择后再冻结确认，保留所有回退、初始化和CPU成本；覆盖站不删，累计RF和打断预算不重置。

归档审阅另发现上一清单的finalize_log.txt哈希在日志仍写入时采集，160项中只有该项不匹配。保留原清单和日志，新的归档需在所有命令结束后单独计算清单并核对，不能把该清单瑕疵隐去或称旧160项全通过。
