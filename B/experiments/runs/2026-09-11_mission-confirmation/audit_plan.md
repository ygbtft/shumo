# 本轮归档核查

仅在B/写入。已完成2145次冻结后确认，不再调参或生成计分场景。

1. 11策略×4场景精确重放44条完整反馈轨迹；请求必须逐条一致。重放完成后才读评分真值，检查每次角锥/成对无信号裁剪仍包含源；认证清除时先用公开区域或near反馈核对证书。
2. 独立重算2145条轨迹的接收、180度方向、5m近源、20m光学、同点固定反馈、两位角度和每动作移动/切频/检测计时。方向恰好处于浮点边界时明确使用容差，不冒充任意精度认证。
3. 验证新6423次计分执行的完整性、停止原因、预算和冻结哈希；数学检查、HTTP核查、CLI smoke不重复计入执行数。
4. 核验原题、同学论文原图、公开探索和两个上游clone未改变；连续覆盖证书、光学法锥证书和Q2区间上界记录齐备。
5. 保存新的final_checks和artifact_checks，再保留旧16131 manifest并更新到22554。后续场景97—106仍保留，不读取生成。

命令见同目录command.sh的audit/finalize；不连接任何HTTP服务。若断言失败，保留日志并修正原因，不能抹掉失败或计作通过。

归档后的已见回退诊断：用diagnose对uniform/iid/93的fast22和quarter22再作逐请求重放，按实际source/survey任务拆分移动、检测、切频与光学成本。只读取已有公共反馈，不读评分真值；不是新确认或新计分执行。实际区域半径来自重放策略的当前集合，包含成对无信号裁剪，避免只重建direction角锥导致区域半径虚高。

实际审计范围补充：本轮640276条确认指令中，同地点重复有效读数计数为0，所以重复反馈一致性断言没有新增样本；不能拿它当固定误差性质的新验证。固定性依据仍是原有误差场规则检查及同学所供记录中的一组同点重复（recording_audit.json），证据来源和边界在REPORT中单列。
