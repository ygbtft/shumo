# 新策略冻结后确认：seed87—96

2026-09-11，三轮新增4278次训练全清，选择11个固定候选后再生成新场景。

Q3保留九点论文布局代理、七点快速联合、七点最近保证清除位置、九点最近保证清除位置。更完整保收和连续角度minimax在训练中未明显降低整局均值，不强行纳入推荐；Q2理论和全部负结果仍保留。

Q4比较旧方格45保守、独立公开16上限的22点联合、四分之一探测及150m共享冷却、按首个实际探测位置预测任务、最近保证清除位置，以及0.15/0.30探测分位的清除位置版。覆盖普通均值、压力尾部与CPU的不同权衡，参数禁止看完确认结果再修改。

普通：三种布局×五种误差×新seed87—96=150场景/问题，随机流42+45+repeat。压力：新的SeedSequence([42,114,problem,layout_index,count])生成5类布局×10/13/16源×正/负/空间对抗误差=45场景/问题，Q4仍含全向和定向两类。共2145次配对执行。此前77—86仅用作已知诊断/机制重放，不混入新确认。

先冻结所有参数、路径、代码哈希与证书，随后生成真值。只读取Client反馈，本地CPU单线程、seed42；所有输出B/。报告普通/压力分别的全清、总/每源均值、P95/最大、指令、CPU/墙钟和逐例回退；补充对强基线的配对比较。240/460目标不改聚合口径。正式请求0，goal不因一项均值提升而完成。
