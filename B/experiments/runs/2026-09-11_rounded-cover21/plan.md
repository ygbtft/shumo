# 对可执行的舍入坐标重做连续覆盖证明

原21站36个候选的坐标是三角函数生成的浮点数。本轮分别按厘米、整数舍入每个站点，然后重新找盲区并做全域分割证书。72候选先冻结，0计分；所有保留坐标必须通过独立全角点/凸包/分割验证。它只检查站点坐标舍入，不把角度舍入误差混为一谈，也不假设官方必按某精度处理位置。

优先从获证整数布局中按静态路线和不同朝向选少量训练候选，完整比较整局费用。静态路线最短不是完整任务最优。最大站点半径<1950、站数21，继续保持原动作上界。CPU单线程、seed42、仅B/写入，原件上游只读，正式请求0。
