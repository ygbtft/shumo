# 相切证书的范围与后续

4个原未决整数配置全部获证；精确整数距离/闭凸包与独立三角形成员检查一致。向内破坏1m产生的真实轴向盲区被正确拒绝。它们依赖闭180°端点口径及既有同点接收假设，不能直接推广到开端点或未知官方数值实现。

轴向多源协议预检已经完成，180条完整预检轨迹及9个夹具保存在cover21-training中，独立于计分。正式端点口径仍待用户提供实际环境后验证；当前正式请求0。

主候选grid21_29具有严格余量，不依赖相切等号。相切closed21_3的新确认普通均值比22站range慢0.414%，最坏压力多1890.68秒，因此不因整数证明更精细或静态路线更短而推荐为主方案。

未来在坐标舍入、细分深度或半径阈值变化时重核整数范围和距离余量；不得只放宽浮点容差。理论细节见../../../INTEGER_COVERAGE_GUARANTEE.md。本目录计分执行0，整体goal继续active。
