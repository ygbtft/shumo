# 正确处理闭180°边界与相切布局

舍入搜索的4个未决布局，外圈凸包的轴向支撑恰好1800m，未找到漏检点。严格凸包内缩证书会把合法的边界相切当作未决。此处固定4候选，使用整数坐标/整数几何和闭凸包判定；接收距离仍保留10^-5m内缩。独立三角形包含与完整整数分割证明覆盖连续圆域。

不改原始搜索的4个未决，不靠放宽浮点容差接纳，不把任意未决都称为覆盖。通过后才允许策略训练。只写B/、CPU单线程、seed42、0计分、0正式请求。
