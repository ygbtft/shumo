# 保持原计划的静止RF删除

对应Q3面积/完工代理、Q4限幅/服务弧/固定站序四个无跳过基线，保留内部计划频道和历史尝试，只有原地且所有可行源必超出1500m才省略RF。真实客户端状态和真实反馈不改；到新位置的第一条请求保留。证明展开计划等价、请求子序列和频道切换三角不等式，目标是虚拟时间/指令逐例支配明确基线。CPU不作保证。

先做372次关闭功能重放、372次展开逻辑计划检查，再用实际双协议核对时间/位置；训练12候选×93旧场景，之后冻结新确认。检查不算计分。仅B/写入、CPU单线程、seed42、正式请求0。
