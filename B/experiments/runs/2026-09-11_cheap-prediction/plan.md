# 保持负反馈预测决策的廉价预筛

2232次延后扫描训练全清；双负点预测Q3未删任何额外请求且增加CPU，Q4有小额删除收益但CPU增加。独立增加标量必要条件：只用P的第一个真实顶点先排除明显不满足距离/线段条件的负点对，最终所有接纳仍调用原完整P五半平面证书。平方距离留1e-5m²、线段参数留1e-4余量，近零行列式不作快速拒绝。

先400随机+80旋转平移/边界对比原函数，再6种预测完整558条已用训练反馈及删除计数等价，随后12方法×93=1116实际配对测CPU。代码复制前一measure方法但替换证书函数；不通过全局运行时补丁改变任何旧策略。137—146未生成，正式请求0，CPU单线程、seed42，只写B/。
