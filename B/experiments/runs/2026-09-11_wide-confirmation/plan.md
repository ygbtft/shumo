# 宽探测、受限横移与区域先验：新场景确认计划

先完成1674次宽角度和1581次横移/清除成本训练，再冻结11候选，随后才生成107—116与新压力流。普通150、压力45/问题，合计2145次计分执行。基础seed42、CPU单线程、只写B/、正式请求0。

Q3保留论文九点布局代理和当前clearance7，确认面积先验+后续路程，以及扩展候选+同一评分；后者训练普通更慢但压力略低，保留取舍而非预定获胜。Q4保留quarter22、packet_center22、clearance_f015_22三种现有参照；确认5度分包、40m横移、按区域半径控制横移、0.15步长40m横移。选择只依赖67—71及已见压力训练。

普通seed=42+65+repeat（0..9），压力位置SeedSequence([42,116,problem,layout_index,count])，误差seed=42+2000+100*problem+index。五种压力布局×10/13/16源×正/负端点及空间相关误差。此轮确认不能用于候选调参；117—126留作后续。

检查两轮训练的完成计数、285与267项检查、决策源文件哈希、统计字段无重名，然后冻结。确认完成后逐例配对比较全清、每源算术平均、总时间/P95/最大、指令、CPU/墙钟和初始化；保留所有回退。独立重放反馈、审计全部轨迹物理与计时、连续覆盖证书、累计RF和打断预算、原始输入只读、本地自建随机端口HTTP及CLI。新宽探测上界335136s与9766指令，不能套用旧窄角界。
