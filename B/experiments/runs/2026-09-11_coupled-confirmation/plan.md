# 任务联动与负反馈：新场景确认计划

本轮在1674次联动、1116次负反馈和744次等价加速训练后冻结10候选。Q3保留论文九点布局代理、area_return1_7，比较严格距离跳过与固定站序插入；Q4保留packet_center22、width40_f015_22，比较距离跳过、加速服务入口/出口路线、加速固定扫描顺序，以及一项加速负反馈组合以检查新几何的迁移。选择均依据67—71和已见压力训练，不据新场景改参。

普通seed117—126=42+75+repeat（0..9），压力SeedSequence([42,117,problem,layout_index,count])，误差seed42+2300+100*problem+index。每问题150普通、45压力，10候选共1950次计分。先冻结源码/参数再生成真值；127—136留待下一轮。基础seed42、CPU单线程、仅B/写入，正式请求0。

保持335136s和9766指令预算。独立重放全部发生负反馈区域收缩的已见训练案例和新确认案例；检查已知频道距离跳过的连续区域下界、真实位置及客户端状态不被伪改。全部确认轨迹独立重算物理和时间；给出配对均值、总时长/P95/最大、逐例回退、指令、CPU/墙钟/初始化。HTTP只访问本进程创建的随机本地端口。

上一清单的finalize日志在哈希采集时尚未写完，已核对仅此1项不匹配，旧清单/日志完整保留。本轮最终清单在日志关闭后生成，并逐项重算确认，不能再把正在写的输出文件纳入旧哈希。
