# 保留首次冻结的确认：只修结果采集

目标、11候选与原task-confirmation/plan.md相同。首次冻结后生成首个q3/uniform/iid/97场景，但因initialization_s重复字段而在首条计分行前退出。旧attempt保留，未读取其性能结果、未调参；所有算法哈希与首次冻结逐一核对。

新builder删除policy.stats的重复计时字段，由确认外层独立测量初始化；先在两条已见训练轨迹做采集修复等价检查，再运行97—106及原定新压力流完整2145次。明确区分“所有参数先于首次真值生成冻结”与“重启目录的快照在首个fixture之后”；配置不伪称后者也在全部数据生成之前。

覆盖、10轮/20RF累计、最多24次打断与有限光学兜底不变；采样仍只决定排序。所有统计含失败/回退、初始化/墙钟及尾部。仅B/写入，CPU、seed42，正式请求0。
