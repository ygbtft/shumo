# Q3完工路程与区域先验，Q4有界横向距离

宽探测首轮1674训练全清，但过大的固定角度增加移动。Q4新增20/40/60/100m横向距离目标（半角仍限制1.02—30°），另试0.15纵向分位、随当前MEC半径收缩的宽度、较少打断。用首轮5°分包作为强训练基线。

Q3比较把候选点到MEC中心的预计后续路程加入评分；该中心是位置代理，不是真值或保证剩余代价。另比较均匀区域面积先验：正三角求积节点给出精确面积一二阶矩，非线性测向后验评分仍是近似；不把有界区域改成概率保证。扩展候选始终通过全向保收证书，且相对首次锚点被限制在x∈[-150,1652]、y∈[-277,277]，矩形直径<1900m，仍在原Q3动作域内。每源最多3次主RF及原光学兜底。

面积矩检查采用矩形/三角形解析值、点/线退化及80个多边形加边中点的不变性。零评分改动应与旧Q3控制93条反馈逐请求一致。所有新方法做完整压力协议检查后冻结。17候选×93=1581已见训练，107—116与新压力不生成；仅B/，CPU单线程、seed42派生，正式请求0。
