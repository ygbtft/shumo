Add NTJ B research workspace, experiments, and Q1/Q2 benchmark audit

Preserve the independent B algorithms, reports, complete numerical experiments,
reference snapshots, and known correctness failures in a dedicated B directory.
Include read-only inputs and probe dependencies needed by the historical checks.

Add a publication inventory with SHA256 verification. Exclude machine caches,
temporary pytest files, nested Git metadata, and process ID files. Existing
upstream files are unchanged; optimization remains paused and no official
simulator test is run by this publication.
