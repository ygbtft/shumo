# 发布前检查

适用 /Users/dingdingzai/Desktop/AGENTS.md 已读取，认证内容未复制到本次日志。B/ 没有额外 AGENTS.md。用户授权范围为新建并 push NTJ_B，所有本地写入仍仅限 B/。当前工作区非 Git 仓库，使用隔离 clone。

远端 b SHA 已查询；NTJ_B 不存在。网络在沙箱内无法连接本机代理，已通过网络权限重试读取成功。已有 Git 提交身份可用；gh 和 git-lfs 不可用，不安装工具。现有最大实验 JSON 为 52.38 MiB，小于 GitHub 单文件 100 MiB 限制。不会将大型完整结果删除来缩减上传。

B/.swift-module-cache 约 199 MiB；不属于研究结果。7 个 pytest 临时绝对符号链接将随临时目录排除。新算法实验不启动，原暂停状态完整保留。
