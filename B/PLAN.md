# B 题独立算法与离线实验计划

基础随机种子 42；CPU；Python `/Users/dingdingzai/anaconda3/bin/python`。
所有新文件仅写入 B/。遵守 `/Users/dingdingzai/Desktop/AGENTS.md`；用户的 B/ 唯一写入约束优先于其中的通用实验目录规定。

1. 完整读取四页题面及两份 DOCX（含公式、表格、图），提取只读副本并记录原件 SHA256。
2. 独立实现半平面交、退化/无界判断、直径与最小包围圆；用解析反例和独立数值求解交叉检查，计入 0.01° 返回精度。
3. 审核旧模拟器，建立仅提供四种协议反馈的本地环境及策略接口；覆盖固定误差、计时、方向、边界及未知总数。
4. 证明保底覆盖与有限光学清除、结束条件；实现完整方格、裁剪/路线优化、三角格改进、主动定位并保留兜底。
5. 在固定场景集比较随机与定向边界/半径最小/近共线/极端误差/总数未知等情况；记录真值仅用于场景生成与离线评分，策略只能读取反馈。
6. 做独立几何检查、协议检查、失败反例及消融；保存命令、配置、逐次记录、耗时和图表。
7. 在 REPORT.md 给出数值收益、条件性最坏保证、官方验证缺项和选 B 建议；不调用官方接口、不注册、不消耗测试机会。

用户追加偏好：加入智能算法。增加种群 96、精英保留、顺序交叉、逆序变异和 2-opt 局部改良的遗传路线算法；固定种子 42，与最近邻和 2-opt 分开消融。主动定位采用反馈驱动的有限假设信息收益优化，明确先验与最坏保证的区别。

实验记录位于 `B/experiments/runs/2026-09-10_independent/`。正式实验前先保存 command.sh。禁止把本地合成清除率解释为官方成绩或获奖概率。

2026-09-10至09-11追加：按用户明确要求建立算法优化 goal。实现免疫、蚁群、离散粒子群、烟花、增强遗传、插入贪心、迭代局部搜索和反馈驱动的动态重排。固定点集优化与完整任务分开评价，同学benchmark上执行配对比较、压力测试与冻结选择后的新种子确认。详细计划、预算、选择规则、原始轨迹和结果保存在 `B/experiments/runs/2026-09-10_metaheuristics/`；全部CPU、基础种子42、正式连接0。
